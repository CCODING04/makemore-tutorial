# S1 学习报告 · Part 7（minimind / 现代 LLM）

> 审计人：学生 agent S1（数学畏难型：每个推导步都要能跟上、符号都要有定义）
> 材料：tutorial/ 全部 7 个 .md + scripts/ 13 个 + assignments/assignment_7/assignment.md
> 实跑：8 个脚本（6 通过、2 超时中断），无 env-blocked。日期 2026-09-04。

## 总分：8.3 / 10

一句话：**数值例子和"可复算的账本"是全教程最强项（手算例、形状链、实测表几乎逐位可复现），但"同一配置在不同章节数字是否一致"这条失分明显——kv 头数、MoE 专家数、rope θ、中间维度算例各有 2~3 处互相打架，另有 SwiGLU"0~1 软开关"这类与自身数表矛盾的表述。**

---

## 卡点清单（🔴 在前）

### 🔴 红色（数字/事实冲突，学生照着学会记错）

1. **minimind 的 KV 头数跨章冲突**：README 演进表与 03 章都断言"minimind 用的正是 **8 Q 头 / 4 KV 头**（2:1）"，作业 Q3/Q5 也说"minimind 采用 8Q/4KV"；但 **05 章配置表明确写"minimind2-small(26M) 是 8/**2**"，并特意警告"26M 的 kv_heads 是 **2**（不是 4）——GQA 压得更狠"**。8/4 实为 64M minimind-3 / 本课 GPU 模板（脚本 05 GPU 分支 n_kv_heads=4）的配置。学生读完 03 记 8/4，读到 05 被当面纠正。
2. **特殊 token id 与真实脚本不符（01 章）**：教程引用的"核心代码"是 `special_tokens=["<|endoftext|>", "<|im_start|>", "<|im_end|>"]`、`BPE(unk_token="<|endoftext|>")`，预期输出块写"endoftext(0), im_start(1), im_end(2)"。**实际脚本 `01_bpe_tokenizer.py` 的 `SPECIAL_TOKENS = ["<|im_start|>", "<|im_end|>"]`、`BPE(unk_token=None)`**；实跑+读回 tokenizer 验证：`im_start → id 0、im_end → id 1、endoftext → None（根本不在词表）`。id 是 04 章 loss masking（eos）的承重墙，错一个数就 masking 错位。
3. **MoE 配置三方各说各话**：教程 03 说"minimind 用 **4 专家 / top-1**"（示例代码 `num_experts=4, top_k=1`）；**脚本 04 自己的文档串说"minimind 用 8 experts / top-2，本脚本 4/2"**（实际实例化也是 4/2，输出打印 top_k=2）；05 章配置表又说"minimind-3-moe = **4 专家 top-1**"。三处三个说法，无一注明"这是哪个版本"。
4. **中间维度算例算错（05 章）**：文中给官方公式 `int((π·hidden/64)+0.5)·64` 并注 **"（512→2432）"**。代入 512：int(25.13+0.5)×64 = **1600**；2432 是 hidden=**768** 的结果（int(37.70+0.5)×64=38×64）。学生拿公式一对就对不上。

### 🟡 黄色（数学表述不严谨或自相矛盾，能读懂但会硌一下）

5. **SwiGLU 门控值域说错（03 章，与同章数表矛盾）**：正文两处称 silu 门是"**0~1 之间的软开关**""每个维度有独立的 0~1 门"。但 silu(z)=z·σ(z) 值域不是 [0,1]——**同章的数值表自己就印着 silu(3)=2.86、（脚本 04 实跑还有 silu(4)=3.93）**。silu 的真实卖点是"平滑、负区不归零"，不是"压到 0~1"。
6. **RoPE"相对位置"性质缺关键一步（02 章）**：从"q、k 各自旋转 mθ、nθ"跳到"内积只依赖位置差"，中间缺一行矩阵证明 `R(mθ)ᵀR(nθ) = R((n−m)θ) ⇒ ⟨R(mθ)q, R(nθ)k⟩ = ⟨q, R((n−m)θ)k⟩`（作业 Q2 提示里反而有，正文没有）。且 02 章"为什么 RoPE 更好"第 1 条有一句**没写完的病句**："绝对位置完全不影响（都旋转不改变夹角差的部分...严格说内积依赖角度差）"——省略号像编辑残留，畏难学生读到这句会卡住。
7. **KV Cache 复杂度"总量式"不成立（03 章 + 脚本 03 + 作业题 7）**：站得住的说法是教程前半句"**每步** O(T²)→O(T)"；但后半句"生成 N 个 token，从 O(N²·L) 降到 O(N·L)"不对——带 cache 每步仍要读 t 个历史 key，N 步注意力总量是 O(N²)（无 cache 是 O(N³)）。正确总量应写 O(N³·L)→O(N²·L)。另外符号 **L 没有定义**就出场（读者要猜是层数）。作业题 7 的"总 O(T²)→总 O(T)"是同款松表述。
8. **rope θ 说法打架（02 vs 05）**：02 章说"theta 通常取 1e4~1e6，**minimind 取 1e4**（与 Llama 一致）"；05 章配置表给 minimind2-small 与 minimind-3 都是 **1e6**。两处都咬定"minimind"，没有交代版本差异（本课脚本默认 10000.0，倒是和 02 一致）。
9. **中间维度公式两个版本被称"等价"（03 章）**：03 写 `ceil(hidden·π/64)·64`，脚本/05 用 `int(...+0.5)·64`（四舍五入）。hidden=768 时两者同为 2432，但 **hidden=512 时 ceil 给 1664、round 给 1600，并不等价**——"效果等价"只在部分取值上成立。
10. **"本课 CPU 版"一词两义（05 vs 01-04 章）**：05 章配置表"本课 CPU 版 = hidden 64 / 2 层 / 4Q2KV / 词表 256 / ~0.3M"（对应脚本 02/04/05 的 CPU_MODE 分支）；而 01-04 章叙事的"CPU 缩小版"是 **~26M（hidden 512 / 8 层 / 6400 词表，04 章还给出 val≈2.0 的 5000 步预期）**。同一个词指两个模型，学生不知道自己"正在跑的"是哪一个。
11. **06 章 MLA 数字冠名失实**："DeepSeek-V2 的真实数字（32 层/128 head_dim/seq 2048/fp16）"——脚本 12 自己的标题是"**LLaMA-7B 级：32 层**"。7.0% 这个比值只在 32 头设定下成立（576/8192）；真 V2 是 128 头（比值 ≈1.76%）。账本内部自洽（我逐 token 复算过），但"真实数字"三字不实。

### 🟢 绿色（小瑕疵，顺手可修）

12. BPE 平局规则"取最靠前/**字典序最小**"把两种不同 tie-break 混写；且作业题 1 考的编码侧规则（"rank 最小优先、平局取最靠左"，rank=merges 列表下标）教程只讲了训练侧"频率最高先合并"，**"rank"这个概念在教程里没定义**。
13. 文件名漂移：04 章"产出 `pretrain_hidden.pth`" vs 同章表格 `pretrain.pth` vs 05 章 `out/pretrain_512.pth`。
14. 03 章 Flash Attention 前后**同一段"🔧 Part 9 CUDA"引注原样重复两遍**（编辑残留）。
15. 01 章"To be, or not to be"对照表写"字符级 19 个整数"，示例列表实际 18 个 id（有逗号版 19 字符、无逗号版 18 字符，两版混用）。BPE=6 个 token 的结论本身实测正确。

---

## 分章评分表

| 章节 | 分 | 数学/推导 | 数字例 | 一致性 | 短评 |
|---|:---:|---|---|---|---|
| README | 9 | — | 好（ln6400≈8.8 的预期锚点） | 差（演进表 8Q/4KV 冠给 minimind，与 05 冲突） | 路线图和"看趋势别死记数字"的诚实声明极佳 |
| 01 BPE | 8.5 | rank 规则训练侧清楚、编码侧没点名；两个手推玩具例（low lower / aaabdaaabac）完整 | 好（压缩率 3.43≈教程≈3.5 ✅） | 差（特殊 token 代码+id 全错位，🔴2） | "重叠不重用/平局"细节是全网少见的认真 |
| 02 RMSNorm+RoPE | 8.5 | RMSNorm 手算例完美；RoPE 2D→复数→rotate_half 讲清，但相对位置证明跳一步+病句（🟡6） | 好（[1,2,3] 手算、cos0.5≈0.878 全对） | 中（rope θ 与 05 冲突 🟡8） | "实数版讲原理、复数版看代码、作业两者都收"的处理很体贴 |
| 03 GQA/FFN/MoE | 8 | repeat_kv 形状链逐步注释、KV cache 拼接+start_pos 坑讲透；silu"0~1"错、O(N·L) 松、MoE aux 公式符号含糊但方向对 | 好（KV 缓存 67/33/8MB 三档账我复算全对） | 差（MoE 配置三方打架 🔴3；公式 ceil/round 🟡9） | 信息密度最高的一章，也是数学瑕疵最多的一章 |
| 04 训练流水线 | 8.5 | loss masking 的 a_start−1 对齐推导详细且正确；DPO 公式与代码一致，mean-vs-sum 给了理由（脚本 08 确实用均值 ✅） | 好（ln6400≈8.76 初始 loss 锚点） | 中（文件名漂移 🟢13） | "labels[t]=input_ids[t+1] 为什么要 −1"这种坑肯写出来，对畏难学生是救命的 |
| 05 复现指南 | 7.5 | 外推四件套的机理解释（PI 为何训练内也崩）是全教程最好的因果论述 | 数字最密但**恰好是算例错误所在**（512→2432，🔴4） | 差（配置表本身对，但被前面章节架在矛盾位） | 实测表（11/13 号脚本）逐位可复现，含金量最高；扣分在两处硬数字错 |
| 06 MLA/NSA | 8 | 解耦 RoPE 的"为什么压不进 latent"讲得干净；NSA 三分支表清楚 | 好（1.07GB/0.27GB/0.08GB 实跑复现） | 中（"DeepSeek-V2 真实数字"冠名失实 🟡11） | 5 秒钟的脚本撑起一整章，性价比高 |

---

## 只改 3 件事

1. **全教程统一 minimind 配置口径**（一次改动救四个卡点）：以 05 章配置表为唯一事实源——26M 版 **8Q/2KV、rope θ=1e6、中间维度 512→1600**；8Q/4KV 明确标注为"minimind-3(64M)/本课 GPU 模板"。同步修 README 演进表、03 章"minimind 用的正是 8/4"、脚本 04 文档串"minimind 用 8 experts/top-2"、MoE"4 专家 top-1"的版本归属，以及"（512→2432）"算例（改 768→2432 或 512→1600）。
2. **修 01 章特殊 token**：把教程引用的代码改成真实脚本的 `SPECIAL_TOKENS=["<|im_start|>","<|im_end|>"]`（或在脚本里真加上 `<|endoftext|>`），预期输出块改成实测的 `im_start(0)/im_end(1)`；04 章 eos 引用同步核对。
3. **补 RoPE 相对位置的一步证明 + 删病句**：在 02 章"数学"节加一行 `⟨R(mθ)q, R(nθ)k⟩ = qᵀR((n−m)θ)k`（旋转矩阵正交性），替换"（都旋转不改变夹角差的部分...严格说…）"那句残句；顺手把 SwiGLU 两处"0~1 软开关"改为"平滑、负区不归零的软门控（门值不限幅）"。

---

## 最喜欢 3 处

1. **01 章 `aaabdaaabac` 手推**：把"合并创造新相邻对""aaa 只取前两个 a（从左到右不重用）""平局取靠前"三个最容易含糊的细节全摆在明面上——我第一次真正看懂 BPE 训练和编码为什么是两回事。
2. **02 章 RMSNorm 的 [1,2,3] 手算例**：两种归一化并排算到小数点后三位，还点破"LayerNorm 输出必以 0 为中心、RMSNorm 保持正负形状只压尺度"——一行代数顶十行形容词。
3. **05 章 + 脚本 11 的四方案外推实测**：我复跑出的 ppl（5.00/14.06/5.08/5.27 @128；6.37/13.35/5.20/5.05 @256）与教程表格**逐位一致**，"PI 必须配微调不是论文套话，是实测""YaRN 外推区反而低于自己训练内"这两条解读把表读活了。可复现的数字就是最好的老师。

（备选表扬：脚本 03 的 KV Cache 等价性验证 max diff 5.07e-07；脚本 10 的 gini/max-mean 指标设计——"每一列都能手工复算"的自觉值得所有教程学。）

---

## 费曼自检：RoPE（直觉 → 公式 → 数字）

**直觉（能讲给畏难的自己听吗？）**：把"位置 m"翻译成"把 q 和 k 在平面上转 m·θ 这么多角度"。旋转不改变向量长度（范数），只改变方向；注意力打分用的是内积 = 长度 × 夹角，所以位置信息只会通过"夹角"影响分数。两个向量各自转了 mθ 和 nθ，它们的**相对转角**就是 (n−m)θ——绝对位置 m 是多少不重要，差多少才重要。✅ 教程 02 章这层讲到了。

**公式（每步能跟上吗？）**：
- 2D 旋转：`x₀' = x₀cosθ − x₁sinθ；x₁' = x₀sinθ + x₁cosθ`，等价于复数 `(x₀+ix₁)·e^{iθ}`。✅ 有。
- 多维：hidden 两两分组，第 i 组频率 `freq_i = θ_base^(−2i/d)`，位置 m 转角 `m·freq_i`——高频对转得快管局部、低频对转得慢管远距（教程只说"和傅里叶分解同理"，对畏难学生这里其实值得多两句：为什么指数间隔像"不同进制的钟"）。🟡 略跳。
- 实数实现：`q∘cos + rotate_half(q)∘sin`，其中 rotate_half 恰好实现 i·z。✅ 有，且明确"教程用实数版讲、脚本用复数版、两者等价"。
- **相对位置（关键一步）**：`⟨R(mθ)q, R(nθ)k⟩ = qᵀ R(mθ)ᵀR(nθ) k = qᵀ R((n−m)θ) k`，用的只是 `R(a)ᵀR(b)=R(b−a)`。❌ 正文缺这行（作业 Q2 提示里有）。
- 语言建模为什么在乎：相邻 token 分数高、远的低，且换到序列第 5 位还是第 500 位结论不变——泛化到没见过的绝对位置。✅ 有。

**数字（自己算一遍）**：θ=0.5 rad/位置、q=k=[1,0]：位置 2 vs 3 → cos(1×0.5)=cos(0.5)≈**0.878**；位置 5 vs 6 → 同为相差 1 → **0.878**（绝对位置不同、结果相同！）；位置 2 vs 8 → cos(6×0.5)=cos(3.0)≈**−0.99**（隔远几乎反向）。教程例子的三个数值我复算全对。再看脚本 02 实测：相同相对偏移的内积最大偏差 4.77e-07，整条序列平移 c=2 后注意力矩阵最大偏差 **1.43e-06**——"只依赖相对位置"从口号变成了可验证的不变量。✅ 闭环。

**自检结论**：跟着教程我能对任何人讲清 RoPE 的直觉和数字；"从旋转到内积只依赖位置差"这一步目前要靠我自己脑补正交矩阵性质——这正是"只改 3 件事"第 3 条要补的那一行。

---

## 附录：脚本实跑记录（.venv / MPLBACKEND=Agg，scratch：review/scratch/S1_P7）

| 脚本 | 结果 | 关键输出行 |
|---|---|---|
| 01_bpe_tokenizer | ✅ ~20s | `训练完成，实际词表大小: 6400`；`字符级 1,115,394 / BPE 325,208 / 压缩率: 3.430x`（教程 ≈3.5 ✅）；`<\|im_start\|> → id 0、<\|im_end\|> → id 1`（🔴 与教程 1/2/2+1 冲突）；另用 temp/bpe_tokenizer.json 复核 `'To be or not to be' → 6 tokens` ✅、`endoftext → None` |
| 02_rmsnorm_rope | ✅ | `weight=2.0 后输出均方根 = 2.0000 ✅`；`相同相对偏移内积最大偏差 4.77e-07`；`平移 c=2 最大偏差 1.43e-06 ✅` |
| 03_gqa_kv_cache | ✅ | `repeat_kv (1,4,2,8) → (1,4,4,8)`；`KV Cache vs 完整重算最大偏差 5.07e-07 ✅`；脚本仍打"总 O(T²)…总 O(T)"（🟡7） |
| 04_swiglu_ffn_moe | ✅ | `hidden=64 → intermediate_size=192`；MoE demo `num_experts=4，top_k=2`、`silu(4.0)=3.9281`（直接反证"0~1 门"，🟡5）；`aux_loss = 2.1072` |
| 10_moe_load_balance | ⏱ **time-blocked**（180s 超时被杀，exit 143；文档串自述 ~30s 未兑现） | 无输出留存 |
| 11_rope_scaling | ✅ <180s | 四方案 ppl 表与教程 05 **逐位一致**（5.00/14.06/5.08/5.27；6.37/13.35/5.20/5.05）；`温度 √(1/t)=0.1·ln(2)+1=1.0693`；自检 1/2 全过 |
| 12_mla_nsa_accounting | ✅ ~5s | `MHA 1.07 GB / GQA(kv8) 0.27 GB / MLA 0.08 GB（7.0%）✅ 逐 token 复算与公式一致`；`NSA max diff = 2.3101`——与 06 章完全一致 |
| 06_pretrain_pipeline | ⏱ **time-blocked**（185s exit 124，stdout 缓冲未落盘） | 5000 步训练类脚本按规则 3 分钟中断 |

env-blocked：无（data/input.txt 在库，1,115,394 字符）。13_long_context_eval.py 因时间预算未跑（05 章引用其数字，未验证）。

*概念检验均先自答再对照（BPE rank、RMSNorm 三区别、RoPE 证明步、repeat_kv 头对应关系 i//n_rep、KV cache 复杂度、silu 值域、MoE aux=α·N·Σf_i·P_i 均匀时=α）：7 问中 5 问教程能独立支撑自答核对，2 问（RoPE 证明步、编码侧 rank 规则）需要作业区或自己补证明。*
