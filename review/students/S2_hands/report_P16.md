# S2 学习报告·P16（图像/视频生成：DDPM 与跨模态对齐）

> 审计角色：学生 S2（实操薄弱型：教程照抄能过、脱离步骤就卡）。
> 审计日期：2026-09-04 · 总耗时 ≈15 分钟。
> 材料：`courses/Part16_image_video_generation/`（4 个 md + 2 个脚本）+ `assignments/assignment_16/` 三件。未读 assignment_reference/，未联网。

## 总分：8.8 / 10

结论先行：**教程、脚本、作业三方一致性是本部分最强项**——教程声称的每一处实测数值（loss 轨迹、均值偏移、方差比、√ᾱ 表、CFG 余弦表）我全部本地逐位复现成功；两个脚本零修改跑通（GPU <1 min / 纯 CPU 仅 6.4 s）；作业 4 题（含 stretch）一次全 PASS。主要扣分在**权重下载操作指引缺位**：显存档位讲得很好，但从 `pip install` 到"跑通第一张图"之间的下载/镜像/缓存操作对实操薄弱学生是断崖。

## 卡点清单（按卡的程度排序）

| # | 卡点 | 位置 | 严重度 | 说明 |
|---|------|------|--------|------|
| 1 | 权重下载只给档位不给操作 | README 环境表、02 章 §2、03 章 §3 | 高 | 显存占用（SD1.5 fp16 ~2GB / SDXL ~7GB / FLUX fp8 ≈12GB / CogVideoX-2B fp16 ~4GB、int8 3.6GB / Wan2.1-1.3B 8.2GB / HunyuanVideo ~60GB→量化 24GB）标注完整 ✅，但**没有一条下载命令**：无 `huggingface-cli download` 示例、无 `HF_ENDPOINT` 镜像变量、无缓存目录/磁盘占用汇总、diffusers 无版本 pin（只写"latest"）。弱网/离线学生无法判断"哪一档我能拿到、怎么拿"。模型 ID 陷阱（runwayml 已删→镜像 ID）讲得好，但那是"选哪个 ID"，不是"怎么下载" |
| 2 | 02 章练习 1 的 idx 映射无解释 | 02 章 §动手实践练习 1 步骤提示 | 中 | `idx = min(int(t0 / 30 * 399), 399)`——推理步 30 与训练扩散 T=400 是两套时间线，为何这样线性映射、为何 strength=1 时取 399 而非 400，零注释。我第一眼看不懂，只能当作"教程定义的黑盒"跑通（数值倒是对的） |
| 3 | 01 章性能表的 cosine 行不可复现 | 01 章 §性能数据 | 中 | 表标注"DDPM 论文 + 本课开发机实测"，但脚本 01 只有线性 `make_betas`，无 cosine 训练路径（cosine 代码只出现在 md"错误 1 解法"和章末 TODO 练习 3）。cosine 行"3000 步更好"既非论文数据也非脚本可复现——实测来源存疑 |
| 4 | 03 章练习 1"复现脚本 02 的 [3] 号实验"措辞不准 | 03 章 §动手实践练习 1 | 低 | 脚本 02 的 CFG 实验张量布局是 `(2,16,32)`，练习提示代码是 `(1,512)`；复现的是**实验思想**而非同一布局。参考数值（0.7004/0.9803/0.9974/0.9994）对应的是练习提示代码，我实测逐位一致 ✅——只是措辞会让学生对不上号 |
| 5 | 脚本 02 断言弱于教程声称 | scripts/02 `[2]` 号实验 | 低 | 教程 03 §1 说"scale=0 时输出与纯文本条件完全一致"，脚本只断言 `out_s1 ≠ out_s0`（>1e-4），"scale=0 = 纯文本注意力"仅停留在打印文案（作业测试文件里倒是有这条断言） |

## 分章评分

| 章节 | 得分 | 一句话 |
|------|------|--------|
| README.md | 8.5/10 | 导航表/学习地图/环境表清晰；扣分：diffusers 档位有、下载操作无 |
| 01 手写 DDPM | 9.0/10 | 三段数学+实测数值全可复现，"签名说明"块是全教程典范；扣分：cosine 行来源存疑 |
| 02 文生图/图生图 | 8.5/10 | strength=「01 章闭式第二次消费」串讲精彩，练习 1 参考数值逐位复现；扣分：idx 映射黑盒、操作型练习下载指引缺 |
| 03 对齐与视频 | 8.5/10 | IP-Adapter/CFG/视频选型收束干净，24GB 档位表实用；扣分："复现脚本 02"措辞、练习 2 本机无法验证 |
| 脚本 01/02 | 9.5/10 | 双脚本零修改跑通（CPU 6.4s / <5s），输出与 md 声称数值完全吻合 |

## 一致性核对表（教程代码块 ↔ 脚本 ↔ 实测）

| # | 教程声称 | 对应脚本/实现 | 核对结果 |
|---|----------|---------------|----------|
| 1 | 01 md §2 训练代码块（randint/randn_like/q_sample/mse） | 脚本 01 L100-111 逐行一致（B=256） | ✅ 一致 |
| 2 | 01 md"签名说明"：教程三参 `q_sample(x0,t,noise)` vs 作业四参显式版 | 脚本 01 L40 三参（模块级 ᾱ）；作业题 1 四参 | ✅ 差异已提前声明，数学等价 |
| 3 | 01 md 错误 2 解法 `t_mlp(t.view(-1,1).float()/T)`、"本脚本 T=400" | 脚本 01 L60、L26 | ✅ 一致 |
| 4 | 01 md 采样伪代码（t=0 不加噪） | 脚本 01 L69 `zeros_like` 等价实现 | ✅ 一致 |
| 5 | 01 md 实测：loss 1.11→0.21（0.19-0.23 波动） | 我的实测 1.1148→0.2098/0.2266/0.1947/0.2139 | ✅ 逐位吻合 |
| 6 | 01 md 实测：均值偏移 [0.069,-0.011]、方差比 [1.039,1.065] | GPU 实测完全相同（seed 1337）；CPU 重跑亦 ✅ 分布匹配 | ✅ 吻合 |
| 7 | 01 md 头部"CPU 30 秒" | 强制 CPU 计时 **6.4 s** | ✅ 声称保守成立 |
| 8 | 01 md cosine_schedule 代码（错误 1 解法） | 脚本 01 **无** cosine 路径 | ⚠️ 代码块无脚本承载（练习 3 为 TODO）；性能表 cosine 行不可复核 |
| 9 | 02 md SD1.5 两行推理代码 / img2img 代码块 | 无对应脚本（README 已标"—"） | ⚠️ 无法本地验证（需下载权重，见卡点 1） |
| 10 | 02 md 练习 1 参考数值 0.9204/0.6017/0.2737/0.1322 | 本地实算 0.9204/0.6017/0.2737/0.1322 | ✅ 逐位复现 |
| 11 | 03 md §1：scale=0 与纯文本条件完全一致 | 脚本 02 仅断言 s1≠s0；文本等式只在打印文案 | ⚠️ 断言弱于声称（作业测试中有此断言） |
| 12 | 03 md §2：w=7.5 余弦 ≈0.998 | 脚本 02 实测 0.998 ✅ | ✅ 吻合 |
| 13 | 03 md 练习 1 参考数值 0.7004/0.9803/0.9974/0.9994 | 本地实算（seed=1337，(1,512)）逐位一致 | ✅ 复现（但注意是练习代码而非脚本 02 布局，见卡点 4） |
| 14 | README 导航：01→脚本 01、03→脚本 02、02→无脚本 | 与实际文件对应 | ✅ 一致 |

## 作业元数据 + pytest 输出

工作目录：`/home/admin02/Code/WorkSpace/makemore-tutorial-review/students/S2_hands/work/assignment_16/`（三件复制自 assignments/assignment_16/，仅编辑 generation_exercises.py，未看参考答案）

| 题 | 内容 | 一次过? | 提示次数 | 耗时 | 结果 |
|----|------|---------|----------|------|------|
| 1 | DDPM 前向闭式 `q_sample` + `signal_ratio`（30 分） | ✅ 一次过 | 0 | ~3 min | PASS（公式即教程 01 章闭式 + 章末练习 1 提示） |
| 2 | CFG 公式（25 分） | ✅ 一次过 | 0 | ~1 min | PASS（教程 03 §2 公式照抄） |
| 3 | img2img strength→t₀（25 分） | ✅ 一次过 | 0 | ~1 min | PASS（教程 02 §3 `⌊steps×strength⌋`） |
| 4 🌟 | IP-Adapter 解耦交叉注意力（20 分，stretch） | ✅ 一次过 | 0 | ~4 min | PASS（脚本 02 `DecoupledCrossAttention` 照搬；按 `Q.shape[-1]` 泛化 √d，与测试硬编码 32 兼容） |

**4/4 PASS，一次过率 4/4，提示 0 次，总耗时 ≈9 分钟。** 提示全来自教程正文/章末练习/脚本本身——作业难度曲线与教程对齐良好，对"实操薄弱"人设几乎无墙（这也是教程-作业一致性好的直接证据）。

pytest 实录（`/home/admin02/Code/WorkSpace/makemore-tutorial/.venv/bin/python -m pytest test_generation_exercises.py -v`）：

```
platform linux -- Python 3.12.12, pytest-9.1.1
collected 4 items

test_generation_exercises.py::test_ex1_q_sample PASSED                   [ 25%]
test_generation_exercises.py::test_ex2_cfg PASSED                        [ 50%]
test_generation_exercises.py::test_ex3_strength PASSED                   [ 75%]
test_generation_exercises.py::test_ex4_ipa PASSED                        [100%]

============================== 4 passed in 0.60s ===============================
```

## 只改 3 件事

1. **补"权重下载三步"块**（README 环境表后或 02 章 §2）：`pip install` 之后给 `huggingface-cli download <id>` 命令示例 + `HF_ENDPOINT` 镜像变量 + 六个模型的磁盘占用/缓存路径汇总表。显存档位已很好，缺的是"怎么拿到本地"——这是实操薄弱学生从读懂数学到跑出第一张图之间唯一的断崖。
2. **01 章性能表处理 cosine 行**：要么给脚本 01 加 `--schedule cosine` 开关使"实测"可复现，要么把该行来源标注改为"论文声称"，删掉"本课开发机实测"的含混表述。
3. **02 章练习 1 的 idx 映射加一行注释**：说明 `int(t0/30*399)` 是"推理步 → 训练扩散时间线"的线性近似、strength=1 时取末端 399 的原因——当前是黑盒，学生只能照抄。

## 最喜欢 3 处

1. **01 章"签名说明"块**：教程三参 `q_sample` vs 作业四参版的差异在正文提前声明（"二者数学完全相同"）——教程、脚本、作业三方接口对齐，学生零踩坑，这是全教程里最贴心的一段。
2. **02 章 strength =「01 章闭式的第二次消费」**：把 img2img 的 t₀=⌊steps×strength⌋ 直接接回 DDPM 前向闭式，再用练习 1 的 √ᾱ 表把"strength=1 参考图只剩 13% 信号"从陷阱变成数字——章节之间不是并列而是咬合，且参考数值可逐位复现。
3. **作业题 4 的 stretch 测试设计**：未实现返回 None 时优雅 SKIP 不判 FAIL，实现了自动生效——配合脚本 02 可照搬的 `DecoupledCrossAttention`，让"选做题"真正可选而零挫败感。
