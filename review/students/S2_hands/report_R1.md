# S2 学习报告·P13-C01 手写 MinHash + 分带 LSH 去重

- 学生：S2（实操薄弱型：理论背景尚可，几乎没从零手写过代码，不熟集合运算/哈希族/边界情况）
- 时间：2026-09-04，全程约 14 分钟
- 材料：教程 `courses/Part13_data_engineering/tutorial/01_dedup_from_scratch.md` + 脚本 `scripts/01_minhash_dedup.py` + `assignments/assignment_13/`
- 运行环境：仓库外 scratch 目录（`makemore-tutorial-review/scratch/S2_R1/`），venv = `makemore-tutorial/.venv/bin/python`（3.12.12）

## 总分：4.4 / 5

理论推导清晰、实测输出逐行可复现、三个坑全部能亲手复现——这是本课最强的三件事；扣分集中在"记号/口径不统一"这一类工程卫生问题上，对实操薄弱的学生恰好是最容易绊倒的地方。

## 卡点清单

| 级别 | 位置 | 期望 | 实际 | 时长 | 建议 |
|------|------|------|------|------|------|
| 🔴 | 教程"错误 3"L221 | 复现数字与本课语料一致（错误 1 明言"教程数字可复现是底线"） | 写"n=50 的玩具语料跑出 1225 对"，但本课脚本语料是 **14 篇**（10 基础 + 4 dup），我实测复现得 **91 对** = C(14,2)。第一反应是"我是不是跑错版本了" | ~3min | 改成"14 篇语料 → 91 对全对（=C(14,2)）；n=50 时对应 1225 对"，把两个口径都写明 |
| 🟡 | 脚本 L4 / L20 / 实际 | 注释与实际语料数一致 | docstring"~100 篇玩具文档"、段注释"30 篇"、`make_corpus()` 实际 14 篇——**三个数互相打架** | ~2min | 统一为 14 篇 |
| 🟡 | 教程 L63/L86/L334-359 ↔ 脚本 ↔ 作业题 3 | 教程记号与代码签名对齐 | 教程用 `k/b/r`，练习提示 `minhash_signature(S, k=64)`、`lsh_candidates(signatures, b=16, r=4)`；脚本实参是 `num_hashes/NUM_HASHES、bands/rows`，且 `minhash_signature` 还有教程未提的 `seed=7`。照着提示对抄脚本时会错位 | ~3min | 给一张"教程记号 ↔ 代码参数"对照表（k↔num_hashes, b↔bands, r↔rows），并同步练习提示的函数签名 |
| 🟡 | 教程"形状追踪"框 L129-145 | 每步有形状/集合大小标注 | 只画了切分结构，没标 `len(words)=6 → |S|=4 → len(sig)=64`——对我这种不熟集合运算的人，"min 是在 4 个哈希值里取"这层要自己推 | ~2min | 在框内每步补尺寸数字 |
| 🟢 | 脚本 L99（band20 复现时发现） | 参数化打印 | `LSH 16 bands × 4 rows` 是硬编码字符串，改 `bands=20` 后打印头仍显示 16×4，复现错误 3 时差点被自己的输出误导 | ~1min | 打印用 f-string 引用参数 |
| 🟢 | 教程 L16/L113 ↔ 脚本 | "60 行"与文件对得上 | 脚本 140 行（核心算法确实约 60 行，含 main/对照说明）。初见困惑"60 行在哪"，后可自洽 | ~1min | 写"核心算法 ~60 行（全文 140 行）" |

统计：🔴×1，🟡×3，🟢×2。

## 逐节评分表

| 节 | 评分（1-5） | 评语 |
|----|------|------|
| 理论背景（MinHash 概率性质 + LSH 分带推导） | 4.5 | Step1-3 式推导 + Var 公式 + Q1/Q2 概念检验够用；Step2 证明一句带过（对可接受）；`(1/b)^(1/r)` 拐点我核了：14×8→0.719、28×4→0.435，与 ≈0.7/≈0.44 吻合 |
| 代码实现（四阶段管线 + 形状追踪 + 实测输出） | 4.0 | 实测输出 **[1]-[5] 逐行复现一致**（大优点）；扣分：记号不统一、无带 assert 的 lsh_candidates 安全版完整代码（错误 3 只给了 assert 片段，学生要自己拼回去）、打印硬编码 |
| 练习（课后练习 + Assignment 13） | 4.5 | 四题一次全过；测试基建好（🌟 未实现自动 SKIP、独立运行/pytest 双模式）；扣分：题面函数签名与教程提示、脚本签名三处不完全一致 |
| 避坑（三个真实错误） | 4.5 | 错误 2/3 我都亲手复现成功（见下），叙事"宽进严出"极好；扣分：错误 3 的 50/1225 口径与本课 14 篇语料不符（🔴） |

## 错误复现结果

### 原版脚本 vs 教程"实测输出"：✅ 逐行一致
在 scratch 目录跑原版脚本，`[1] 2 对 / [2] 3 对（含 doc03-dupC 误报）/ [3] 2 对 / [4] 0.69≈0.73 / [5] 14→12` 与教程 L151-157 完全相同。复现性设计（zlib.crc32）是真的。

### 错误 2（空集 Jaccard=1.0）：✅ 复现成功
```
shingles('hi there') = set()
shingles('ok bye')   = set()
jaccard = 1.0
```
词数 2 < k=3 → `range(0)` → 空集；两个空集按约定 = 1.0。一行命令即见，教程给的命令可直接照抄。

### 错误 3（候选对爆炸）：✅ 复现成功，但发现口径出入
跑 scratch 里的 `01_minhash_dedup_bands20.py`（唯一改动：`bands=20`）：
- `[2] LSH 候选对`：3 对 → **91 对** = C(14,2)，14 篇语料**全部两两成对**，爆炸确认；
- 越界 band 切片切出空 tuple `()` → 所有文档"同桶"，静默发生（无 IndexError）；
- `[3]` 验证后仍是 2 对——Jaccard 验证步把爆炸误报全部挡掉，且脚本末尾 assert 不触发（全候选当然无漏报）——正好亲手验证了"宽进严出"；
- **口径出入**：教程写"n=50 的玩具语料跑出 1225 对"（=C(50,2)，数学上没错），但**本课语料是 14 篇**，按本课复现应得 91 对。且该脚本打印头仍显示"16 bands × 4 rows"（硬编码）。

## 作业元数据

| 题 | 一次过? | 提示次数 | 耗时 | 结果 |
|----|---------|----------|------|------|
| 1 Jaccard/shingles | 是 | 0 | ~4min（对照脚本 L49-57 抄的） | PASS |
| 2 签名一致率 | 是 | 0 | ~1min | PASS |
| 3 LSH 概率 + 反推 bands | 是 | 0（j=1 边界对着 docstring 想了一下，最终 b=5） | ~3min | PASS |
| 4 簇消解 keep-first | 是 | 0 外部提示；**借了作业思考题 Q2 的反例**，才免于写成"逐对丢第二个" | ~5min | PASS |
| 🌟 5 keep-longest | 未做（按任务要求） | — | — | SKIP ⏭️ |

未触发 data 路径错误（本作业纯标准库，不需要 data 目录软链）。

### pytest 输出
```
test_data_exercises.py::test_ex1_jaccard PASSED                          [ 20%]
test_data_exercises.py::test_ex2_signature PASSED                        [ 40%]
test_data_exercises.py::test_ex3_lsh_prob PASSED                         [ 60%]
test_data_exercises.py::test_ex4_cluster PASSED                          [ 80%]
test_data_exercises.py::test_ex5_stretch SKIPPED (🌟 Stretch 未实现...)   [100%]
========================= 4 passed, 1 skipped in 0.01s =========================
```

## 只改 3 件事

1. **修错误 3 的数字口径**（🔴）："n=50 语料 1225 对"改为"本课 14 篇语料 → 91 对（=C(14,2)）全对；n=50 对应 1225 对"——教程自己立了"数字可复现"的规矩，这里必须守。
2. **统一记号与语料数**：加一张 k/b/r ↔ num_hashes/bands/rows 对照，练习提示签名与脚本对齐（含 seed），脚本 docstring"~100 篇"/注释"30 篇"统一改 14 篇。
3. **补形状标注与安全版代码**：形状追踪框每步加尺寸（6 词 → |S|=4 → 64 维签名）；正文给出带 `assert num_hashes == bands * rows` 的 lsh_candidates 完整安全版，让错误 3 的解法可直接照抄。

## 最喜欢 3 处

1. **三个坑全部可命令级复现**：错误 2 一行 import 就看到 `1.0`，错误 3 改一个参数就看到 91 对全对爆炸——"不是虚构教学案例"这句话我验证过是真的，这种可复现的踩坑记录比十条最佳实践都教得多。
2. **实测输出逐行一致**：跑出来与教程一字不差（含 0.69≈0.73 这种采样数字），对自学者是极强的正反馈——知道自己没跑错。
3. **"候选 3 对 → 验证 2 对"的宽进严出叙事 + 思考题 Q2 反例**：前者让我真正理解 LSH 为什么敢"宁可多报"，后者直接救了我国 4 的实现（差点写成逐对丢第二个）——理论、代码、作业形成了闭环。

## 产出物

- 作业实现：`makemore-tutorial-review/students/S2_hands/work/assignment_13/data_exercises.py`
- 错误 3 复现脚本：`makemore-tutorial-review/scratch/S2_R1/01_minhash_dedup_bands20.py`
- 本报告：`makemore-tutorial-review/students/S2_hands/report_R1.md`
