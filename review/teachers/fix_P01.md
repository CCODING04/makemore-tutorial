# P01（Part 1 Bigrams）修改说明

> 整改人：T2｜日期：2026-09-04
> 原则：教程数字/表述修正优先于结构重写，不重排无关段落；脚本默认运行行为与训练数字不变（仅修正错误文案/注释）；REPO 只读，全部修改落镜像。
> 台账：`ledger/ledger_P01.md`（编号对应关系见台账，此处按编号汇总）

## 一、教程修改

### tutorial/01_introduction.md
| 编号 | 位置 | 原文摘要 → 改后摘要 | 原因（命中来源） |
|---|---|---|---|
| C08 | 概率论表格 | `log(a×b) = log(a) + log(b)` 行内代码 → $\log(a \times b) = \log(a) + \log(b)$ | PLN G2-3 |
| C02 | 概率链"重点记住"后 | 新增一句预告：瞎猜模型 loss = $\ln 27 \approx 3.30$，后文解释来源 | S1🔴2+S3 K2（C1-3 预埋） |
| C04 | 数据集段 | "约 32,000 个名字，包含约 228,000 个字符" → "32,033 个名字 / 196,113 个字符 / 228,146 个 bigram"三口径并列并提示勿混用 | S2🟢8+S3 K4（硬数字错误） |

### tutorial/02_bigram_model.md
| 编号 | 位置 | 原文摘要 → 改后摘要 | 原因（命中来源） |
|---|---|---|---|
| C05 | §2 计数代码后 | 无 stoi 来源说明 → 新增 ⚠️ 段：必须固定词表（'.'=0, a=1..z=26），`set(words)` 动态推导只在数据覆盖全字母时碰巧正确，作业题 1 是隐藏考点 | S2🔴1（小数据集 stoi 错位） |
| C22 | §2 可视化代码 | `savefig('../images/cell011_output00.png')`+`show()` → `savefig('bigram_matrix.png')`；注明教程图为 notebook 存档、脚本 03 实际存 scripts/bigram_matrix.png、重跑不覆盖 | S2🟡4（照抄往仓库写文件+与脚本脱节，C1-5） |
| C11 | §3 归一化 | "转换成概率很简单：每一行归一化" → 先给 $P[i,j]=N[i,j]/\sum_{j'}N[i,j']$ 与行和含义，点明=条件概率 $P(j\mid i)$，再进 Broadcasting | S1🟡1 |
| C08 | §3 演算块 | 分式演算（1/6、4/15）在代码块内 → 形状对齐示意保留，分式部分改为文字+行内 LaTeX | PLN G2-2 |
| C08/C16 | §5 推导块 | 四步推导挤在一个普通代码块 → LaTeX 分步推导（似然/log/取负/平均各 $$ 单行公式+文字"为什么"），第 4 步补"除以 n"理由（可比性/量级稳定） | PLN G2-1 主违反 + OLD 卡点9 残留 + S1🟡2 |
| C15 | §5 NLL 代码后 | 无警告 → 新增 ⚠️：此处 P 未平滑，全量训练集不炸但遇未见 bigram（andrejq 的 jq）即 log(0)=-∞，解法见下节；注明脚本 05 实测 2.4544（N+1 口径） | S2🟡5 + S1🟢2 |

### tutorial/03_neural_network.md
| 编号 | 位置 | 原文摘要 → 改后摘要 | 原因（命中来源） |
|---|---|---|---|
| C22 | §1 可视化 | 声称"完整脚本见 06"且片段写 ../images → 注明教程图为 notebook 存档、脚本 06 不含绘图代码；imshow 改 xenc[:100] 防卡顿 | S2🟡3（C1-5） |
| C10 | §2 | 只有流程图 → 补 softmax 公式 $$\text{softmax}(z)_i=e^{z_i}/\sum_j e^{z_j}$$ + exp 三理由 + "软版 argmax"点题 | S1🟡4 + PLN 审计要点3 |
| C09 | §2 代码前 | one-hot 定义直接跳 xenc@W → 新增 3×3 手算：[0,1,0]@W=W 第 1 行，"查表/pluck"，W 每行=该字符作为上下文的 27 个分数，与计数版查 N 行对照 | S1🔴1 + S3 默写卡壳1 |
| C23 | §2 初始化 | `W = torch.randn(...)` → 注释注明脚本 06 固定种子 2147483647 | S2 ⚠️ |
| C07 | §3 loss 后 | 无术语说明 → 新增 💡：CrossEntropyLoss = softmax+NLL 合并算子，此处手写即 NLL 部分，混称不焦虑 | S1🟡7（C1-7） |
| C02 | §3 训练循环前 | 无基准线 → 新增"三根基准线"表：瞎猜 $\ln 27\approx3.2958$（全零初始化实测）/ randn ≈3.76 及为何更差 / 计数版 ≈2.454 | S1🔴2 + S3 K2 |
| C13 | §3 脚本指引 | 未说明与脚本差异 → 注明脚本 07 唯一差别是 loss 多 `+0.01*(W**2).mean()`，打印值略高 | S2 ❌ loss 定义不一致 |
| C01 | §3 结论 | "训练后 loss 应该收敛到约 2.47" → "实测 100 步/lr50/λ0.01：3.7686→2.4901（含约 0.03 正则项）；无正则 300 步 ≈2.46 逼近 2.454；旧数字 2.47 对应无正则更多步设置" | S1🟡3+S2🟡2+S3 K1（复现失败） |
| C17 | §3 L2 小节 | "等价于 N+λ"断言 → 补半步推导（W→0 ⇒ e^0=1 ⇒ 均匀 = λ→∞ 极限）+ ⚠️ 类比非严格等价（高斯先验 vs Dirichlet） | S1🟡5 + S3 K6 |
| C03/C12 | §4 对比代码 | stub 注释"它们几乎一模一样！" → 可运行分布对比（P_nn vs P_count，print max 偏差），注明 300 步 ≈0.044 → 3000 步 ≈0.005 | S1🔴3 + S2 ❌ 承诺未兑现（C1-6） |
| C03 | §4 结论 | "W.exp() 和 N 几乎一模一样" → "分布等价非数值相等"：等价部分（行分布偏差→0.005）与不等价部分（W.exp() [0,89] vs N [0,6763]，corr 3000 步仅 0.34）并列，给出 softmax 行内平移不变 ⇒ $W_{i,:}=\log P_{i,:}+c_i$ | S1🔴3 + S3 K5 |
| C18 | §4 MLE 段 | "隐式地最大化似然"断言 → 补一句行归一化频率是多项分布 MLE 闭式解 | S1🟡6 |
| C06/C24 | §4 新增小节 | 无采样对比 → 新增"面试考点：loss 接近 ≠ 分布接近"：计数版（seed 2147483647）vs NN 版 100 步（seed …+10）采样并排表 + λ=0/300 步 NLL 2.4593 仍出乱码反例 | S3 K3 |
| C07/C03 | §4 对照表 | 损失函数列 CrossEntropy/NLL 混用；洞察句"W 最优解=log(P)" → 两列统一 NLL + 术语备注；洞察改为分布等价 + $W_{i,:}=\log P_{i,:}+c_i$ | S1🟡7 + S3 K5 |
| C26 | 预告 | 无论文钩子 → 补 Bengio et al. 2003 及"Part 2-3 是其最小复现，建议对照阅读" | S3 K7/V3（C1-4） |
| — | §4 Q1 提示 | 微调：补"最优的分布相同（W 多每行常数自由度）"；Q2 提示补 ln(27)≈3.296 呼应基准线 | 随 C02/C03 一致性 |

### tutorial/README.md
| 编号 | 位置 | 原文摘要 → 改后摘要 | 原因 |
|---|---|---|---|
| C25 | 配套资源后 | 无环境自检 → 新增"🧰 环境自检"节（torch 版本 + data/names.txt 行数=32033） | PLN C1-2 |
| — | 学习路线 | "每节大约 15-30 分钟"未改 | 时长类 → disputed D01 |

## 二、脚本修改（默认行为与训练数字均不变，已逐一对拍）

| 编号 | 文件 | 原文摘要 → 改后摘要 | 验证 |
|---|---|---|---|
| C05 | scripts/02 L18-20 | 无字符集警示 → 注释 ⚠️：set(words) 动态推导的隐患与固定词表写法 | 实跑输出与原版一致（27 字符 / 228146） |
| C27 | scripts/06 L41/L56 | "W…等价于 bigram 计数矩阵"/"exp 等价于 N 矩阵" → "训练收敛后扮演计数角色"/"伪计数，收敛后行归一化分布才逼近频率分布" | 实跑通过 |
| C03 | scripts/06 等价性打印 | "当 W 的最优值 = log(N) 时，两者完全等价！" → "softmax(W 行)=P 行，W 行=log P 行+行内常数；等价的是分布而非矩阵数值" | 实跑打印新文案正常 |
| C01 | scripts/07 L68 | 硬编码"最终 loss ≈ 2.47" → "≈2.49（100 步+L2 0.01 典型值，含约 0.03 正则项；去正则练更久逼近 ≈2.454）" | 实跑 2.4901，训练日志与原版逐行 diff 一致 |
| C17/C20 | scripts/07 L54 | "L2 正则化等价于…（N+1）" → "类比于…（N+λ）：都往均匀拉（不同先验，非严格等价）" | 随 07 实跑 |
| C06 | scripts/07 总结 | "自动学到同样的结果" → "收敛到与计数版等价的分布"；末尾新增两行采样质量警告（loss 接近≠分布接近） | 实跑输出确认 |

## 三、作业修改（assignments/assignment_1/）

| 编号 | 文件 | 修改 | 原因 |
|---|---|---|---|
| C14 | README.md 文件结构 | `assignment.md # 本文件` → `README.md` | S1🟡8+S3 K7（文件名不符） |
| C21 | README.md 运行测试 + test 跳过分支 | 补 pytest 运行方式；跳过打印明确"pytest 下显示 PASSED 实为跳过"；README 加 ⚠️ 说明（不改跳过逻辑，保双模式兼容） | S2🟢7 |
| C05 | README.md 题 1 思考 | 新增第 3 问：为何用固定词表而非 set(words) 动态推导 | S2🔴1 |

## §验证记录

1. **脚本实跑**（scratch/t2_P1/，python=REPO/.venv，MPLBACKEND=Agg）：
   - 原版 07 基线：步骤0=3.7686 → 步骤99=2.4901，尾部打印"≈2.47"（矛盾复现）；采样 mria/mmyazzieelend/ryalarethrstendrlen/aderedieli/jely。
   - 镜像 07：EXIT=0，训练日志与原版逐行 diff 为空（数字不变），尾部新文案与教程新数字一致。
   - 镜像 02：EXIT=0，输出 27 字符 / 总 bigram 228146（与原版一致）。
   - 镜像 06：EXIT=0，初始 loss 3.7590 不变，等价性打印为新文案。
   - 锚点复算：全零初始化 loss=3.295837=ln(27)；N+0=2.4540、N+1=2.4546（向量化口径；脚本 05 逐词 float32 累加为 2.4544）；纯字符 196,113 / bigram 228,146。
2. **作业测试**：`assignment_reference/assignment_01/bigram_exercises.py` + 修复后 test 拷入 scratch/t2_P1/hw，`python -m pytest … -v` → **5 passed**；直跑模式 → **5 通过 0 失败**（题 5 真实训练，50 步 loss 2.4971，非跳过）。
3. **LaTeX 检查**：`check_latex.py` 跑 4 篇教程（01/02/03/README）→ 全部"✅ 未发现问题"（0 问题）。

## §disputed 清单（详见台账）

1. **D01（P1）**：README"每节 15-30 分钟"时长低估——时长类按约定记台账不动，建议 T0 改为 02/03 章各 45-60 分钟。
2. **D02（P1）**：roadmap L134"脚本 07 从 ≈3.3 收敛"与 randn 起点 3.77 不符——docs/ 不动，教程侧已用基准线表澄清。
3. **D03（P2）**：docs 侧 assignment.md 文件名残留引用——作业侧已修（C14），docs 全局替换留 T0。
4. **D04（P2）**：02 §4 采样"版本差异"免责声明当前环境多余——保留（对未来版本仍有效），删否留 T0。
