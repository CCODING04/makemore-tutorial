# 修改说明 · Part 3（训练诊断与 BatchNorm）整改（T2）

> 整改人：T2 · 日期：2026-09-05
> 输入：plan_P03（T1 预审，18 个 C1 缺口）+ S1/S2/S3 三份 report_P03 + T0 必修清单（10 项）
> 原则：REPO 只读；全部修改写镜像；脚本默认档行为与输出逐位不变；无 wontfix，3 项 disputed

## 一、镜像修改文件清单（16 个）

**教程（4）** `REVIEW/courses/Part3_batchnorm/tutorial/`
- `01_diagnosis.md` — ln(27) 数学式移出代码块改 LaTeX（G2）；初始 loss 补实测锚点（26.78/26.01）；`b2=randn*0`→`zeros` + b1 口径说明（C14）；饱和阈值 0.97→0.99 + 口径注释（C04）；单层实拍图替换深层网存档图、片段改存当前目录（C25/C11）；新增「5/3 是怎么来的？」推导小节 + fan_in/fan_out 定义 + 一行验证代码（C09）；`std=5.3→√30` 理论链 + 实测 5.57（C17）；练习 2/3 阈值与题干方向修正（C10）
- `02_batchnorm.md` — **BN 前向 LaTeX 公式**（μ/σ²/x̂/y，ε 在方差上）（C07）；内联代码改 `var(unbiased=False)`+eps（C07/C12）；γβ 示意图数字修正 -1,1,3,5,7（C16）；running 统一存 **var** + 视频口径 ⚠️ 框（C12）；新增 **「σ 用 1/n 还是 1/(n-1)？」三口径表小节**（C07）与 **「预习：γ/β 的梯度」小节 + Part 4 Step 12 衔接链接**（C08）；类实现补 `requires_grad=True`（C13）；「在 MLP 中使用」补最小上下文（C27）；推理改 `sqrt(bnvar_running+eps)`（C12）；坑 2 重写（eps 只防除零、batch=1 统计无意义）（C20）；坑 3「200000 步」→ 脚本 20000 + EMA 时间常数（C05）；新增 2 张实跑图 + mean/var 数值表（C24）；b1 三口径说明框（C15）；[-2,2] 补"约95%"（C26）
- `03_deep_network.md` — **末层去 BN + ⚠️「以 scripts/05/06 为准」框 + 初始 loss≈3.91 实测**（C02）；「初始化技巧」重写为 Karpathy 原版对照、本脚本无此步（C03）；风格框补「示例输出均为脚本 06 实跑」+ 种子口径说明（C30）；工具 1-4 示例输出全部换 06 实跑（C06）；工具 3 图注修正 + 新柱状图 + 健康标准量化（C18/C19）；工具 4 片段改脚本实际 ud_ratio_history 写法（C32）；总结表同步
- `README.md` — 新增「🛠️ 运行脚本」节（STEPS/Agg 用法）（C31）

**脚本（6）** `REVIEW/courses/Part3_batchnorm/scripts/`
- `01`-`06` — 统一 `STEPS` 环境变量短程档 + 周期打印 flush，默认档**逐位不变**（01/02=1000、03=10000、04/05=20000、06=1000），docstring 注明烟测用法（C28）
- `03` — dev 评估复用已训练基线参数，删除无谓的二次 1 万步重训（同种子输出不变）（C29）
- `05` — 删除死变量 `update_data_ratios`（C29）

**作业（3）** `REVIEW/assignments/assignment_3/`
- `README.md` — **题 1 预期改实测口径（≈26，20~30 区间）**，对齐测试断言 >10 与脚本 01 的 ≈26.8（C01）；题 5 双口径（测试 10k 步/<2.5、挑战 200k 步/<2.1 不强制）（C22）；`assignment.md`→`README.md` + pytest 用法（C21）；ln(27) 3.298→3.296（C23）
- `test_batchnorm_exercises.py` — 仅注释/打印 3.298→3.296，**断言零改动**（C23）
- `batchnorm_exercises.py`（学生骨架）— docstring/打印 3.298→3.296，TODO 骨架不动（C23）

**图（+4）** `REVIEW/courses/Part3_batchnorm/images/` — 全部由脚本同款配置实跑数据生成、英文图内标签 + 中文图注 + 正文数值表：
- `01_tanh_saturation_single.png`（脚本 02 配置，饱和率 65.05% 与 S1 实测逐位一致）
- `02_bn_standardization.png` / `02_bn_running_stats.png`（脚本 04 配置 2000 步，pre 0.054/3.329 → norm 0.000/1.000 → γβ 0.010/1.009）
- `03_grad_data_ratio.png`（**镜像 06 副本实跑 dump 数据重绘**，与教程表格逐位一致：log10 = -2.41/-1.99/-1.82/-1.85/-1.84/-1.80/-1.29）

**台账** `REVIEW/ledger/ledger_P03.md`（32 条 fixed + 3 条 disputed）

## 二、验证记录（全部实跑，scratch/t2_P3/）

1. **关键断言**（verify_claims.py）：题 1 = **26.0063**、题 2 = **3.3078**；`nn.BatchNorm1d` 的 running_var == EMA(**无偏**)方差（True），biased 为 False，n=32 时两口径比 31/32=0.96875；std(tanh(N(0,1)))=0.6277→1/std=1.593、`calculate_gain('tanh')`=1.6667；**脚本 05 结构（末层无 BN）全训练集初始 loss=3.9119**；题 5 参考答案 @10000 步 batch64 = **2.2296**（<2.5 ✓）。
2. **默认档不变性**：镜像 03/04/05/06 全量档实跑，与 S1 报告记录**逐位一致**——03：3.2747/2.3522、3.2953/1.8805、dev 2.2835、饱和率 14.94%；04：step20000=1.9160、2.1904/2.2002/2.2021；05：2.1453/2.1625/2.1628；06：tanh_0-4 std/饱和率、ud log10 全部吻合（run0{3,4,5,6}_full.log）。
3. **短程档**：6 脚本 STEPS=100 烟测全部 exit 0（补丁后无语法/行为问题）。
4. **test 注释口径核实**：脚本 01 同款配置（seed42、b1 randn）全训练集初始 loss = **26.7891** ≈ 注释的 26.78，注释保留、仅精度 3.298→3.296。
5. **作业 pytest**：修复后 test + REPO 参考答案（assignment_03）→ **5 passed**（4.36s，含题 5 拓展实跑）。
6. **check_latex.py**：4 篇教程 + 作业 README 共 5 个改动 md → 全部「✅ 未发现问题」。

## 三、Disputed 清单（详见台账）

- **D01 难度/时长标注**：Part2 README 无星级体例，Part3 单方面加会破坏一致性；时长 4-6h→8-10h 属 roadmap 全局裁量，留 T0。
- **D02 脚本 06 eval 路径采梯度**：诊断 2/3 用 `training=False` 前向再 backward（与 Karpathy 训练模式口径不同）；教程数字/图已按此口径实跑引用，改动需 T0 批准并同步更新。
- **D03 进阶内容立项**：S3 K3（Santurkar 2018 平滑 loss landscape）与 K4（BN→LayerNorm→RMSNorm 坐标系，为节点 7 铺路）属新增小节，超出纠错授权，留 T0 决定。

## 四、遗留（不在本 Part 范围 / 通用问题候选）

1. 六脚本各自复制 build_dataset/训练循环，无公共模块（与 Part2 相同的结构性重复）；Part3 与 Part2 的 stoi 构建风格链断裂（Part3 跟随 Part1 风格）——数值等价，仅风格问题，建议课程层统一。
2. cell008_output01.png 存于 images/ 但无任何 md 引用（Part3 既有状态，未清理——删图超出授权）。
3. 教程 03 工具 1-3 代码片段仍是类写法（与脚本字典写法的差异已有风格声明 + 实跑输出对照缓解）；若 T0 希望"片段可直接跑出脚本输出"，需把片段整体改写字典风格，动作较大留议。

## 五、好写法候选（供其他 Part 借鉴）

1. **02 章 momentum 双口径说明框**（既有）：内联 0.001 与 PyTorch 0.1 并存且讲清收敛速度差异——本次在旁边补齐了 σ 三口径表，两框凑成"统计量口径"教学对。
2. **03 章「先看更新比率、再回溯」诊断顺序 + -3 标尺**（既有）：本次补上量化健康标准与实拍柱状图后，四个工具全部"有代码、有实跑数字、有图"。
3. **STEPS 环境变量短程档约定**（延续 Part2）：默认档逐位不变、docstring 一行烟测用法，高负载/低配机器验证成本从分钟级降到秒级。
