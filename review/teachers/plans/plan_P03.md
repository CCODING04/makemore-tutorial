# Part 3（BatchNorm）分章审计计划 · T1 主教预审

> 预审范围：`REPO/courses/Part3_batchnorm/`（tutorial 4 篇 md + scripts 6 个 + images 4 张）、`REPO/assignments/assignment_3/`（README/test/exercises 骨架）、`REPO/docs/course_roadmap_v3.md` 节点 3（L160-183）。
> 本计划只列审查点与初步证据，正式审计时逐条核实，不改课程文件。

---

## 0. 总体印象

骨架健康：诊断→处方→工具箱的三幕结构清晰，与 roadmap 节点 3 的"教程 3 章 + 脚本 01-06 + 作业 3 + 论文"四件套对齐。主要风险集中在三类：①教程代码与脚本实现存在多处口径冲突（末层 BN 有无、方差有偏/无偏、b1 三种处理）；②作业 README 的预期数值与测试断言直接矛盾；③02 章 0 配图、脚本生成的 3 张图无人引用。

---

## 1. C1 标题-内容对应表

### 01_diagnosis.md「训练诊断：你的模型生病了」

| 标题承诺 | 实际小节 | 缺口 |
|---|---|---|
| 前置知识：MLP 亚健康 | ✅ 回扣 Part2 ~2.17 | — |
| 症状 1：初始 Loss 过高 | ✅ 期望 vs 现实 / 怎么修 | **缺口 1.1**：修复写法 `b2 = torch.randn(vocab_size) * 0`（L43）应作 `torch.zeros`，教学上易误导；修复片段未交代 `b1`（脚本 01 基线 `b1 = torch.randn` 与 Karpathy 原版 `b1*0` 不同，教程未提） |
| 症状 2：tanh 饱和 | ✅ 定义/诊断代码/梯度消失图解 | **缺口 1.2**：饱和阈值不统一——正文与脚本 06 用 `>0.97`，脚本 02/03 与作业题 4 用 `>0.99/0.999`；**缺口 1.3**：L117 配图 cell015 是深层网络（5 条 Tanh 曲线）的图，本章语境是单隐藏层 MLP，图文错位 |
| 解药：Kaiming 初始化 | ✅ 核心思想/gain 表/√fan_in 直觉/对 MLP 意义 | **缺口 1.4**：gain=5/3 无任何推导来源（仅"直觉：方差放大 fan_in 倍"），未点 5/3 是 tanh 二阶导相关的经验收敛增益（Karpathy 原话），也未引 He et al. 公式；gain 表本身质量好（含 Leaky ReLU/Sigmoid） |
| 课后练习 ×3 | ✅ | **缺口 1.5（硬伤）**：练习 3 题干"为什么 tanh 的 gain 比 ReLU 的 √2 小吗？"是事实性错误——5/3≈1.67 **大于** √2≈1.41，题干方向反了，且提示文本（"tanh 压缩到 [-1,1] 而 ReLU 只压负半轴"）推导出的结论恰与题干相反 |

### 02_batchnorm.md「BatchNorm：深度学习的维生素」

| 标题承诺 | 实际小节 | 缺口 |
|---|---|---|
| 为什么需要 BN | ✅ 初始化只在初始时管用 | — |
| BN 原理（三步） | ✅ 标准化→γ/β→running stats | **缺口 2.1**：全章公式只以 Python 代码呈现，无 LaTeX 数学式（对比 roadmap"论文三个必推细节：有偏方差、dσ² 项、ε 位置"——本 Part 一处未提，dσ²/反传推迟到 Part 4 可以接受，但有偏方差在本章就该讲）；**缺口 2.2**：内联代码 `x.var(0)`（无偏）与脚本 04/05/06 `var(..., unbiased=False)`（有偏）冲突，也与论文细节冲突 |
| 从零实现 | ✅ 类实现 + MLP 内使用 + 推理 | **缺口 2.3**：同章双口径——内联版 running 统计量是 **std**（`bnstd_running` EMA 的是 std），类实现和脚本是 **var**（`running_var`），Karpathy 视频用 std 并口头注明"应改用 var"，教程未做任何说明；**缺口 2.4**：`b1` 处理三种口径——教程内联版无 b1（对，BN 后 bias 冗余）、脚本 04 保留 `b1*0.01`（注释"会被吸收"）、作业题 5 要求 `b1=zeros`，审计时需统一说法 |
| BN 的坑 ×3 | ✅ 忘切模式/bs=1 NaN/running 未收敛 | 质量好，是教程亮点；坑 3 的"全量重校准"代码可作审计示范点 |
| 全章配图 | — | **缺口 2.5**：0 张图（仅 2 处 ASCII 伪图），标准化前后分布对比、running stats 收敛曲线、γ/β 变换效果均无实图；脚本 04 也全程不产图 |

### 03_deep_network.md「深层网络与诊断工具」

| 标题承诺 | 实际小节 | 缺口 |
|---|---|---|
| 构建深层网络 | ✅ Linear/Tanh 类 + 6 层结构图 | **缺口 3.1（重点）**：教程 6 层代码与 ASCII 图均含输出层 `BatchNorm1d(vocab_size)`，但脚本 05/06 末层线性层**不加 BN**（`i < len-2` 才加），Karpathy 原版末层也无 BN——教程与脚本、与原版三方面冲突；**缺口 3.2**：「初始化技巧」`layers[-1].gamma *= 0.1`（L85）在 Karpathy 原版是对末层 **Linear.weight** 乘 0.1，教程改成 gamma 且脚本 05 里**根本没有这一步**——承诺的技巧在脚本中不存在 |
| 4 种诊断工具 | ✅ 激活/梯度/grad:data/update:data + 总结表 + 诊断顺序 | **缺口 3.3**：诊断片段用 `isinstance(layer, Tanh)` 类风格，脚本 05/06 是 dict 风格，虽有 L78 风格说明但片段无法与脚本对照运行；**缺口 3.4**：工具 3 的配图（cell017）实为权重梯度**直方图**，标题/alt 写"参数梯度比率"，比率本身无对应图（脚本 06 有柱状图但未嵌入）；**缺口 3.5**：工具 3 健康标准只有"各层接近、无极端值"，未给数量级参考（工具 4 有 -3 参考线，工具 3 应补"各层 log10 梯度 std 同数量级"之类标准） |
| 课后作业 / 预告 | ✅ | — |

### README.md「Part 3 总目录」

导航表、路线图（Part2→Part3→Part4）、5 条学习目标、作业链接、论文/视频链接、上下章链接均齐。**缺口 3.6**：无章节难度/时长标注（roadmap 节点 4 明确提到 Part4 README 有难度星级表；Part3 若课程体例要求星级表则缺失，审计时对照 Part2 README 体例定论）。

### 作业层（assignment_3）

- **缺口 3.7**：任务指定的 `assignment.md` 不存在，实际文件是 `README.md`，且 README 内文件结构图自称 `assignment.md # 本文件`（L43）——名实不符。
- **缺口 3.8（最高优先级）**：README 题 1 预期"loss ≈ 3.7 ~ 4.0"，而 `test_batchnorm_exercises.py` L36 断言 `loss > 10.0` 并注明"实测史实 ≈26.8"。学生按 README 预期自检会误判自己写错。README 与测试必有一个错（实测 26.8 与 Karpathy 一致，README 错）。
- 审计点（非缺口）：README 题 3 要求 gamma/beta 设 `requires_grad=True`，教程/脚本的 BatchNorm1d 类靠外部 `parameters()` 循环补设——学生照抄教程类可能不过测试，需核对测试是否实际检查该属性。

**C1 缺口合计：18 处**（01 章 1.1-1.5 共 5，02 章 2.1-2.5 共 5，03 章及 README 3.1-3.6 共 6，作业层 3.7-3.8 共 2）；其中 **1.5（练习 3 题干方向反了）与 3.8（作业预期值与测试断言矛盾）为事实性错误级**，正式审计最高优先处理。

---

## 2. 章级学生单元划分与难度

| 单元 | 内容 | 预计时长 | 难度 | 说明 |
|---|---|---|---|---|
| A | 01·症状 1 初始 Loss | 20-30min | ★☆☆ | 只需 ln(27) 概念，Part2 可直接衔接 |
| B | 01·症状 2 + Kaiming + 练习 | 60-90min | ★★☆ | 练习 2 需跑 4 组 gain 对比实验 |
| C | 02·BN 原理 + 双模式 + 三坑 | 90-120min | ★★★ | 面试手写题核心；std/var、momentum 细节多 |
| D | 03·深层网络搭建 | 30-45min | ★★☆ | 结构清晰，但有 3.1/3.2 冲突需先解决 |
| E | 03·诊断工具 4 件套 + 练习 | 60-90min | ★★★ | ud ratio 概念抽象，需配图理解 |
| F | 作业题 1-4 | 1.5-2h | ★★☆ | 单次 forward 级，快 |
| G | 作业题 5（拓展） | 0.5-1h 机器时间 + 调试 | ★★★ | steps=200000 默认值，CPU 约 30-60min |

难度曲线合理（★★☆→★★★→★★★），roadmap 总预算 4-6h 偏紧：单元 B+C+E 纯阅读已超 3.5h，加作业实际 8-10h——审计时核对 roadmap 时长标注是否要修正。

---

## 3. 本 Part 特有审计要点（正式审计逐项打勾）

1. **激活统计诊断（tanh 饱和率）**
   - [ ] 阈值统一化裁定：0.97（教程 01/03、脚本 06 注释）vs 0.99/0.999（脚本 02/03、作业题 4）；Karpathy 原版 0.97。
   - [ ] 脚本 02 的"死神经元"统计（单神经元 >90% 样本饱和，L116-118）是超出 Karpathy 的增值内容，教程是否应收录。
   - [ ] 直方图判读教学："挤在 ±1 附近"的读图指引是否充分（教程 01 L119 仅一句）。
2. **Kaiming 初始化推导（gain=5/3）**
   - [ ] 补推导方案：前向方差守恒 Var(Wx)=fan_in·Var(W)·Var(x) → gain²/fan_in；5/3 的来源（tanh 反传方差守恒的经验增益）至少给出处。
   - [ ] 修正练习 3 题干方向（缺口 1.5，事实性错误）。
   - [ ] 核对脚本 03 gain 表数值：√(2/(1+0.01²))≈1.414（Leaky ReLU α=0.01）✔、Sigmoid gain=1 标注"不推荐"是否需解释（Sigmoid 官方 gain=1 因饱和，教程未讲）。
3. **BN 公式逐步推导与反传**
   - [ ] 前向公式 LaTeX 化：$\hat{x}=\frac{x-\mu_B}{\sqrt{\sigma_B^2+\epsilon}}$, $y=\gamma\hat{x}+\beta$。
   - [ ] 有偏方差（除 N）在本章明示（对齐 roadmap 论文细节 1），修复教程 `x.var(0)` 与脚本 `unbiased=False` 的口径分裂（缺口 2.2）。
   - [ ] running 统计量口径统一：std vs var（缺口 2.3），并说明 PyTorch `nn.BatchNorm1d` 用 var。
   - [ ] 反传 dσ² 推导：确认"推迟到 Part 4"在两章均有显式交接说明（Part3 现无任何预告）。
4. **BN 的 train/eval 模式差异**
   - [ ] 三坑小节质量已高，重点核：坑 1 示例代码 `model.eval()` 出现在无 nn.Module 语境（本 Part 全手写层），前后说法是否自洽。
   - [ ] 脚本 04/05 用 `bn1.training = False` 手工切换，教程是否提"为什么不用 model.train()”（无 Module 基类）。
   - [ ] 脚本 06 诊断 2/3 用 `training=False` 的前向再 backward（L225），采到的是 eval 路径梯度，与 Karpathy 训练模式采梯度不同——审计裁定是否算口径偏差并在教程声明。
5. **诊断工具使用**
   - [ ] 工具 3 配图与语义对齐（缺口 3.4）；工具 3 健康标准量化（缺口 3.5）。
   - [ ] ud ratio 公式 `(lr*p.grad).std()/p.data.std()` 教程 L248 与脚本 06 L184 一致 ✔；-3 参考线解释（L259）与脚本 06 L249-251 一致 ✔。
   - [ ] 脚本 05 L165 `update_data_ratios = []` 死代码，审计时列入脚本问题清单。

---

## 4. scripts 运行档位建议

| 脚本 | 训练量 | matplotlib | 预估时长（CPU 单核） | 建议档位 |
|---|---|---|---|---|
| 01 | 2×1000 步 + 全量 forward ×2 | 无 | 1-2min | 全量即可 |
| 02 | 1000 步 + 全训练集统计 | 有（只 savefig 无 show，Agg 安全） | 1-2min | 全量即可 |
| 03 | **3×10000 步**（L151 重复调 `train_model(False)` 多训 1 万步，浪费但同种子结果一致——列为脚本问题） | 有（savefig） | 8-15min | 烟测档 n_steps=1000；全量档 10000 |
| 04 | 20000 步 | 无 | 6-12min | 烟测 2000 步（lr 调度阈值相应改）；全量 20000 |
| 05 | 20000 步深层网 | 无 | 6-12min | 同上 |
| 06 | 1000 步 + 4 诊断 | 有（savefig） | 2-3min | 全量即可 |

通用建议：6 个脚本数据加载/build_dataset 均重复（可提"公共模块"建议但非本 Part 硬要求）；matplotlib 档统一 `MPLBACKEND=Agg`；脚本均用相对仓库的 `../../data/names.txt` 路径（已确认 data/names.txt 存在）；seed 均为 42，而作业用 2147483647——教程数字与作业预期不可直接对照，审计记录在案。

---

## 5. 格式规范预检（违反位置清单）

- **G1 编号点挤行**：未发现（4 篇 md 均无一行多编号点）。0 处。
- **G2 数学进代码块应 LaTeX**：
  1. `01_diagnosis.md` L21-23：`expected_loss = -ln(1/27) = ln(27) ≈ 3.29` ——纯数学式进代码块，应改 `$-\ln\frac{1}{27}=\ln 27\approx 3.29$。（硬违反 1 处）
  2. 边界（可不改）：`02_batchnorm.md` L80-87 训练/评估模式伪代码块、L214-218 NaN 解释注释、`03_deep_network.md` L261-271 log10 刻度 ASCII 图——伪代码/示意图，正式审计裁定豁免与否。
- **G4 应配图**：
  1. `02_batchnorm.md` 全章 0 图（缺口 2.5）——BN 章是全课程最该有分布对比图的地方。
  2. `01_diagnosis.md` L117 / `03_deep_network.md` L115：cell015 图为深层网 5 层曲线，与单隐层/多层语境两处复用，01 处图文错位。
  3. scripts 目录 3 张图（tanh_saturation.png / kaiming_init.png / diagnostic_tools.png）未被任何 md 引用——图-文脱节，尤其 kaiming_init.png（hpreact 前后对比）正是缺口 1.4 需要的图。
- **G10 正文代码可拼凑运行**：
  1. `02_batchnorm.md` L147-177「在 MLP 中使用」：缺 `import torch.nn.functional as F`、`g`、`Xb/Yb`、数据构建前置，且与 L148-151 依赖 Part2 变量——拼凑需跨章，应加最小上下文声明。
  2. `03_deep_network.md` L97-113 与 L141-257 诊断片段：`isinstance(layer, Tanh)` 类风格 vs 脚本 dict 风格（L78 有风格声明，缓解但不解决"拼不出脚本 05 实际输出"的问题）。
  3. `03_deep_network.md` L82-87「初始化技巧」：`layers[-1].gamma *= 0.1` 在脚本 05 中无对应步骤（缺口 3.2），照抄拼不出脚本行为。
  4. 轻：`01_diagnosis.md` L37-44 修复片段（上下文充分，可拼凑）✔；`01/03` matplotlib 片段指向 scripts/06 补全链接已有 ✔。

格式违规合计：**硬违反 7 处**（G2×1、G4×3、G10×3），边界情况 3 处待裁定。

---

## 6. 跨 Part 一致性

**字符映射（stoi 构建风格）**：

| Part | 写法 |
|---|---|
| Part1_bigrams/scripts/03 | `stoi = {s: i+1 for i, s in enumerate(chars)}`（+ 单独 `stoi['.'] = 0`，同 Part3 风格） |
| Part2_mlp/scripts（01/04/06/07 等） | `chars = sorted(set(...)); chars = ['.'] + chars; stoi = {s: i for i, s in enumerate(chars)}` |
| **Part3_batchnorm/scripts（01-06 全部）** | `stoi = {s: i+1 ...}; stoi['.'] = 0` —— **跟随 Part1，不跟随直接前置 Part2** |

- 数值映射三者等价（'.'=0，字母 1-26），无功能差异；但 Part2 用 `['.'] + chars` 风格、Part3 回退 Part1 风格，风格链断裂。审计建议：课程层统一为一种（倾向 Part2 风格，因其显式展示 '.' 的特殊地位），或在 Part3 教程正文补一句"字符表构建回顾"（当前 Part3 教程 md 完全未展示 stoi 构建，正文层面无冲突，仅脚本层不一致）。
- 其余一致性：数据划分 random.seed(42) + 80/10/10 六脚本一致 ✔；block_size=3 全程一致 ✔；脚本 seed 42 vs 作业 seed 2147483647（Karpathy 种子）不一致，影响教程-作业数字对照，列入审计记录。

---

## 7. 正式审计执行顺序（建议）

1. 先裁 3 个事实性/矛盾项：练习 3 题干方向（1.5）、作业题 1 预期值（3.8）、末层 BN 有无（3.1）——这三项影响学生能否跑通/自洽。
2. 再做 02 章逐段核对（BN 口径三分裂：有偏方差、std vs var、b1），对齐脚本 04。
3. 跑脚本 02/03/06（matplotlib 档），核对三张脚本图与教程嵌入图（cell 系列）内容是否互证。
4. G2/G4/G10 按 §5 清单逐条定位行号定稿。
5. 作业测试全绿性复核（pytest assignments/assignment_3/，注意题 5 的 200k 步时长）。
