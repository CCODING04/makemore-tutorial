# Part 19（Agent / Function Calling）整体审计计划 — T1 主教预审

- 预审日期：2026-09-04
- 对象：`REPO/courses/Part19_agents/`（tutorial 3 个 md 共 1060 行：README 107 + 01 章 512 + 02 章 441；scripts 3 个共 1011 行：01_agent_loop.py 498 + 02_mini_mcp.py 216 + 03_tau_mini.py 297）+ `REPO/assignments/assignment_19/`（assignment.md 192 行 + agent_exercises.py + test_agent_exercises.py）+ `docs/course_roadmap_v3.md` 应用线 A2（L555-564）与缺口登记 L595
- 预审方式：只读通读 + 手工核对（教程引用的相对链接 14/14 全实存、教程"逐字实测输出"与脚本 print 格式逐条对读、pass^1 算术复核（1/9=0.11 ✅）、作业接口"同名同签名"声明三方对读、G1 ①②③连排与 LaTeX 逐行勘验、POLICY 字数统计），未运行脚本、未改文件、未联网
- Part 性质：应用线 A2 收官章、"训 agent（P17）→ 用 agent（P19）"的推理侧姊妹篇。质量面：**诚实声明密度全课程顶级**（0.5B 三任务 pass^1=0.00 照实登、幻觉检测阳性、评测方差 0.11 vs 0.00 双运行披露、nudge 第一版踩坑实录、mini-MCP notification bug 实录）；G14 口径声明（device/精度/库版本/解码方式）是全课程样板。主要风险集中在**讲解用节选代码未标"节选"（G10）**、**零配图（G4）**、少量承诺-实现不符（每轮调用数上限、parameters/inputSchema 差异被抹平）与数字口径小漂移。
- 与 P17 的方法差异：三脚本均零 API key、零 Docker——01/03 用本地 Qwen2.5-0.5B-Instruct（首跑需下载 ~1GB，要 HF 网络/镜像），02 纯 CPU 秒级；执行阶段**无 env-blocked 必选项**，但 01/03 的 Section 0/0.5 之外部分在 CPU 上要 6-15 分钟且脚本无 flush、无短程档（见 §四 附注）。外部事实（MCP 规范版本、A2A、Pi 博客、τ-bench 评分 bug、SWE-bench 榜单）离线只能降级核实。

---

## 一、C1 标题-内容对应表（3 文件逐个）

| # | 文件（行数） | 标题 | 实际内容 | 对应脚本 | C1 判定 |
|---|---|---|---|---|---|
| 0 | README.md (107) | Part 19: Agent 与 Function Calling — 手写 Agent Loop、MCP 协议与 τ-bench 评测（应用线 A2） | 学习目标 5 条、理论导览表（6 行逐项链接 ✅）、章节导航 2 行 ✅、前置知识（P8-02/P17-01 必须 + P18 建议 + P14 可选，链接全实存 ✅）、链路位置图（P17+P18→本部分）、环境声明（含 CPU 档与可复现性口径 ✅）、学习地图、作业/资源链接 ✅ | 01/02/03 | **对应 ✅**。5 条学习目标逐条可指认章节；姊妹篇声明 + 应用线收尾导览齐备 |
| 1 | 01_agent_loop.md (512) | 01 — 手写 Agent Loop：while 循环就是 Agent 的全部骨架 | 学习目标 5 条 → 问题引入 → 五部件逐行（2.1 工具层 / 2.2 解析层 / 2.3 回填 / 2.4 nudge+裁剪+循环检测 / 2.5 终止条件表）→ 实测输出（沙箱直测/成功基线/失败恢复/能力边界/假模型验证，标注环境口径 ✅）→ 工程实践（4 陷阱 + 性能表 + 4 最佳实践）→ 概念检验 4 → 练习 2 → 扩展思考 2 | 01 全对应 ✅ | **基本对应，2 缺口**：① 陷阱 3 解法列"每轮调用数上限"，但脚本 01 的 `for c in calls:` **无每轮上限**（脚本 03 的 `calls[:3]` 才是），承诺-实现不符（缺口 #1）；② 2.1 白名单代码块含未定义 `env`/`BASH_TIMEOUT` 且未标节选（归 G10，见 §五） |
| 2 | 02_protocols_and_frameworks.md (441) | 02 — 协议、框架与评测生态：Agent 的世界不止一个 while 循环 | 学习目标 5 条 → §1 协议三层分工表 → §2 mini-MCP（输出实录 + 与 01 章对应表 + 规范四要点 + A2A/AGENTS.md）→ §3 框架四流派表 → §4 多智能体三方辩论 → §5 评测（τ-bench 家族 + τ-mini 实测 + SWE-bench + 评分 bug 社区经验）+ §5.3 agentic RL 去向 → 陷阱 4 → 概念检验 4 → 练习 2 → 扩展思考 3 | 02/03 全对应 ✅ | **基本对应，2 缺口**：① §2.1 表称 TOOL_SPECS 与 tools/list"**同一 JSON schema 格式**"，未提 OpenAI `function.parameters` vs MCP `inputSchema` **外壳键名不同**（脚本 02 用 inputSchema，学生对照即困惑，缺口 #2）；② 政策"~300 字"与实际 614 字符/100 词不符（缺口 #3，脚本 03 L40 注释同源） |

- 导航链完整性 ✅：README 首尾互链、01↔02 互链、两章→assignment_19 路径实存、面试指南 §7b 实存。
- 教程"逐字实测"与脚本对读 ✅（正面标杆）：§3.1 四行沙箱输出、§3.2-3.4 trace 格式（`[turn N] CALL ...` / `⇒ stop_reason=...`）、§3.5 假模型四行（22→15 条裁剪算术正确）均与 print 语句逐字吻合；sorted(WHITELIST)=['cat','grep','ls','python'] ✅；Demo 4 final_answer 截断长度与 `[:100]` 一致 ✅。
- 作业↔教程↔脚本同步 ✅（G17 家族）：01 章 L200 预告"作业 19 题 3 宽口径" ✅；02 章 L189 回指"作业思考题 Q1" ✅；作业实验题的 `NUDGE_BUDGET`/`WHITELIST`/`R` 三个旋钮在脚本中实存（01 L43/46、03 L37）✅；"同名同签名"声明（parse_tool_calls/pass_at_1/TOOL_SPECS/mini_mcp_call 三步握手）与脚本一致 ✅；题 5 未实现 → `pytest.skip` 机制实装（test L19-23）✅ G18 合规。

## 二、学生单元划分（体量 1060 行 < 2K 门槛，整 Part 通读 + 侧重分工）

> 按 master plan §4.1 **不拆章**；三学生同读全 Part，按下表侧重记录。

| 单元 | 学生 | 侧重范围 | 审计重点 |
|---|---|---|---|
| A | S1 数学畏难型 | 01 章 §2.2/§2.5 + 概念检验 Q2/Q3 + 作业题 2/3/4 | 本章数学密度低，S1 的"精确语义"是主战场：`[]` vs `None` vs 抛异常三种失败语义（题 2/Q3）；循环检测严口径（同工具**同参数**×3）vs 作业宽口径（同工具×3）的状态机差异（题 3/Q2）；pass^1 定义、"空列表返回 None"的估计量理由（题 4）。Q3 的 0.7^8≈0.057 独立性假设是否讲透 |
| B | S2 实操薄弱型 | 三脚本全量实跑 + 作业 pytest 三件套 + 实验题三连 | 模型下载档位（HF 镜像）、Section 0/0.5 无 GPU 秒级段先跑；实验题：NUDGE_BUDGET 2→0 看 Demo 3 是否还能自纠、WHITELIST+echo 重跑 Demo 4、脚本 03 跑两次对比 pass^1 方差并改 R=6；pytest 应 4 绿 + 题 5 SKIP（未实现时）；作业骨架 tool_spec 的 inspect 路径 |
| C | S3 面试冲刺型 | README 证据句 + 02 章 §1/§3/§4/§5 + 面试直通车五问 + 跨 Part | 协议三层各举一例；三方辩论数字（15×/4×/90.2%/3-5 subagent）的出处与时效口径；SWE-bench"锁版本+脚手架口径"读法；框架四流派选型边界；P17↔P19 双向分工话术；τ-bench 评分 bug 的"锁 commit hash"教训 |

- README 全局口径（姊妹篇声明、应用线收尾导览、锚点仓库）由主教统裁。
- 三人共同动作：先做后看；脚本先 `cp` 到 `REVIEW/scratch/<单元>/` 再跑（G16）；pytest 在 `REVIEW/students/<S>/work/assignment_19/` 内跑；Demo 3/4 与 τ-mini 依赖模型下载，网络受限时以 Section 0/0.5（01 脚本前半、无模型确定性段）+ 脚本 02 秒级全跑为降级档，如实登记。

## 三、本 Part 特有审计要点（任务点名七项逐一落地）

1. **agent loop 三终止条件**（教程 01 §2.4③/§2.5 ↔ 脚本 01 L255-307 ↔ 作业题 3）：
   - 预审确认三件套设计自洽 ✅：①无 tool_calls（final/nudge 分流）、②max_turns（默认 8）、③循环检测 `len(set(call_hist[-3:]))==1`（(name, sorted-args-json) 元组）——教程"同工具**同参数**×3"与脚本逐字一致 ✅；"同工具不同参数=正常探索放行"的辨析（§2.4③/概念检验 Q3/作业 Q2 三处口径一致 ✅，且互相引用无 G17 错位）。
   - 假模型确定性验证（Section 0.5）是本章教学亮点：循环逻辑与模型解耦的可测性证据 ✅，学生应能复述"换模型零改动"。
   - 学生核对点：nudge 触发条件脚本实现为 `not any_success`（从未有成功结果）而教程表述"所有工具结果都是错误"——零调用时两者等价（空集全错），但"先成功后全错"场景教程措辞会触发 nudge、脚本不会；差异是否值得半句声明，由 B 单元实测后裁量（观察项，非缺口）。
2. **沙箱白名单安全性讲解**（教程 01 §2.1/L110-114/陷阱 3 ↔ 脚本 01 L77-135）：
   - 三道闸（白名单第一词 / 10s 超时 / 400 字符截断）各自防一类事故的对应关系 ✅；"模型输出是不可信输入"原则与 Echo Trap 防御面回指 P17 ✅；"白名单只是第一道门，生产加容器沙箱/只读 FS/审计日志"边界诚实 ✅。
   - 技术细节抽查 ✅：calculator 字符白名单 + 空 `__builtins__` eval（注入面收窄）；白名单只看第一个词的绕过面（`cat x; rm -rf /`？——shell=True 下 `;` 后命令不受白名单约束！脚本未防，教程未提。**这是安全性讲解的真实缺口**：一行 `shlex.split()[0]`+`shell=False` 或至少半句"分号/管道可绕过第一词检查，生产必须 shell=False"即可补（缺口 #4，P1 候选——安全教学点讲防御却留了一个讲解层面未提示的绕过）。学生实测：`bash("cat /tmp/agent_demo.txt; echo pwned")` 验证绕过。
   - 陷阱 3 解法"每轮调用数上限"脚本 01 未实现（缺口 #1，见 §一）。
3. **τ-bench 微缩 pass^1 判分逻辑**（教程 02 §5.1 ↔ 脚本 03 verify() ↔ 作业题 4/Q4）：
   - 判分 = 任务完成 ∧ 未违反政策（DB 终态 + 调用序列双查）✅ 与 τ-bench 口径一致；T2/T3"拒绝类任务真空也 pass"的不作为漏洞由**作业 Q4 提示主动披露**（含堵法三条）✅——诚实度样板；"合规性评测的对象是行为不是话术"与 T2 实测互证 ✅。
   - pass^1 数学 ✅：均值估计量、空列表 None（区分"全挂"与"没跑"）、两组均值加权平均可加性（题 4 验收）、pass^k"k 次全过"定义与 0.7^8≈0.057 独立性演示 ✅；τ²-bench 双控制、τ-bench 用户为 LLM 扮演 vs 剧本的取舍表 ✅。
   - 实测数字可信 ✅：三任务 ×3 全 False→0.00 与另一次 T3=1/3→OVERALL 1/9=0.11，算术正确且"小样本必须附运行次数与温度"的方差教训齐备；"综合自开发期多轮采样观察——单次运行形态会变"的免责到位 ✅。
   - 学生实测：脚本 03 两次运行对比（温度 0.7 方差）；T1 的金额陷阱（89.0 vs 97 含运费 vs "10 天"误当金额）在 verify 的 `abs(...-89.0)>0.01` 判据中定位。
4. **MCP/A2A/AGENTS.md 协议三层讲解准确性**（教程 02 §1/§2.2/§2.3 ↔ 脚本 02）：
   - 三层分工（MCP=agent↔工具 / A2A=agent↔agent / AGENTS.md=agent↔代码库）表述准确且"三层不互替"定位清楚 ✅；MCP 治理归属主动免责"以官网为准" ✅。
   - JSON-RPC 2.0 细节抽查 ✅：notification 无 id 禁止回应（server 侧 `if "id" not in req: continue` 实装）、-32601 方法不存在、工具级 isError 与协议级 error 两级分开、initialize/notifications/initialized 三步时序——与规范一致；protocolVersion "2024-11-05" 为真实版本串且注明"示例用；真实 server 会协商" ✅（后续还有 2025-03-26/2025-06-18 版本，教程未提，属可接受的教学主干取舍；执行阶段可一句带过）。
   - 开发实录（第一版 notification 回响应 → 消息读串）是协议教学的稀缺素材 ✅，与陷阱 1 呼应。
   - 缺口 #2：`parameters` vs `inputSchema` 外壳差异被"同一 JSON schema 格式"抹平（§一）；真实 MCP 尚有 resources/prompts 能力、分页 cursor，脚本注释已声明取舍 ✅。
5. **多智能体三方辩论**（教程 02 §4 ↔ 作业思考 Q1）：
   - 三方立场与出处 ✅：Anthropic（编排者-执行者、3-5 subagent、15×/4× token、90.2% 内部评测）——数字与工程博客披露口径一致且带链接+复述来源 ✅；Cognition 两条定律的转述（上下文传递有损/行动改变结果）忠实 ✅；LangChain 调和（多 agent=上下文工程的手段、广度型 vs 深度型）✅。
   - 本课结论"收益来自上下文隔离而非数量"贯穿 §4/陷阱 2/扩展思考 1/作业 Q1/面试直通车五处口径一致 ✅；"15× 是乘数不是常数"的成本红线是好补充 ✅。
   - G13 裁量：外部数字均不可复现，已有来源链接+定性边界，但宜补披露时点（Anthropic 博客为 2025-06 口径）——见 §五。
6. **与 Part 17 分工声明**：P19 侧 ✅ 无缺口——README L3-9（"17 训 agent / 19 用 agent"）、01 章 L3-7 与 L67-68（"推理侧拼接上下文，训练侧切割 loss"）、02 章 L7 与 §5.3（评测当奖励 = P17 闭环 + 回指 P17 脚本 01）共 5+ 处声明；根 README L47/68/179 双向口径一致 ✅。**P17→P19 单向缺失已由 plan_P17 缺口 #6 登记（P17 侧任务），本 Part 无需动作，执行阶段仅复核 P17 修复落地**。
7. **API key 与档位说明**：三脚本**均零 API key** ✅——01/03 本地 HF `Qwen2.5-0.5B-Instruct`（~1GB fp16），02 零模型。README"环境"节已给档位（GPU 直跑 / 纯 CPU 慢 20-40 倍）与可复现性声明（贪心可复现 vs 温度采样方差本身是教材）✅。执行注意：首跑需 HF 下载（必要时 `HF_ENDPOINT` 镜像）；"~1GB 显存"表述与 fp16 实际吻合 ✅。

## 四、scripts 运行档位建议（执行时先复制到 scratch，G16）

| 脚本 | 档位（docstring/代码实据） | 预计耗时 | 审计动作 |
|---|---|---|---|
| 01_agent_loop.py | GPU 直跑（实测 18.4s，RTX 4090）/ 纯 CPU 慢 20-40 倍（估 6-12 分钟）；**Section 0（沙箱直测）+ Section 0.5（假模型终止条件）不加载模型、秒级、CPU 确定性**；贪心解码同设备可复现；零 API key，首跑下载 ~1GB | 秒-分钟级 | 全量跑（GPU 优先）：输出与教程 §3 逐字比对（同设备应一致）；无 GPU 学生先跑 Section 0/0.5 段并登记降级；实测缺口 #4 的 `;` 绕过与实验题（NUDGE_BUDGET=0 / WHITELIST+echo） |
| 02_mini_mcp.py | 纯 CPU、零模型、秒级（`--server` 子进程自拉起）；零依赖标准库 | <5s | 全量跑，输出与教程 §2 逐字比对；重点观察 Step 4 协议级错误与"线上字节流"段 |
| 03_tau_mini.py | GPU 实测 ~12-25s（docstring 口径；教程写 15-25s，缺口 #5）/ CPU 慢 20-40 倍（估 5-15 分钟）；temperature=0.7 top_p=0.9 有方差；R=3 常量可手改 | 秒-分钟级 | 连跑两次对比 pass^1（教程 0.00 vs 0.11 的方差叙事）；改 R=6 复跑；逐轨迹 verbose 段核对 verify() 三查（get_order 前置/金额 89.0/T2 地址终态/T3 无退款） |
| 作业三件套（pytest） | 纯 CPU 零模型秒级；题 1-4 必答、题 5 🌟 弹性（未实现 `pytest.skip`） | <10s | 未实现骨架应 4 PASS + 1 SKIP（G18 验证）；对齐 reference 前先独立核 assignment 验收标准与测试断言一致；实验题三选一动手 |

- 本 Part **无 env-blocked 必选项**，G2 门禁可全额执行；唯一环境依赖是 HF 模型下载（网络）。
- **附注（G8 风险，非本次点名规范）**：01/03 在 CPU 上 >1 分钟但 print 无 `flush=True`、无短程环境变量档——执行阶段 CPU 学生中断即零输出。建议整改顺手项：给两脚本 print 加 flush 或提供 `DEMOS=0`/`R=1` 类档（P2 顺手项，归整改阶段裁量）。

## 五、格式规范预检（G1/G2/G4/G10/G13/G14）违反位置清单

> 合计 **5 条实锤 + 3 处裁量**。P19 是 G14/G12/G17 的正面样板（口径声明、无失败庆祝、章末-作业指引全对齐），短板在节选代码标注与配图。

- **G1（编号点必须换行）— 2 处实锤 + 2 处裁量**：
  1. 01 章 L317-320（blockquote）："差距不在…而在：① 多步依赖规划…；② 失败后的策略…；③ 对自身行为的校验…" ①②③ 连排同一引用段。
  2. 02 章 L183-185：Cognition "两条基本定律：① 上下文传递有损——…；② 行动会改变结果——…" ①② 各含两层转折仍连排一段。
  3. （裁量）作业 Q4 提示"堵法：①…；②…；③…" 折叠块内连排；02 章 L251-253"三类问题——…" 未编号连排（短列举可豁免）。
  - 另：01 章 L63 ①②③ 在 ASCII 流程图内、L95-105 在代码注释内——图示/代码豁免，不计。
- **G2（数学一律 LaTeX）— 1 处实锤（本章数学极稀，重灾区不在 G2）**：
  1. 02 章 L349（概念检验 Q3 答案）："pass^8 ≈ 0.7^8 ≈ 0.057" 行内计算纯文本，应为 `$0.7^8 \approx 0.057$`（pass^1/pass^k 属指标命名，豁免；整改后须过 `REVIEW/check_latex.py` 0 问题）。全 Part 无代码块承载推导 ✅、无公式内中文 ✅。
- **G4（能画则画）— 1 处系统性违反**：全 Part **零图**（无 `images/` 目录）。天然可画对象：① 协议三层分工示意图（agent/工具/agent/代码库四方关系，§1 表的图伴）；② pass^1→pass^k 衰减曲线（0.7^k，配合 Q3）；③ 三方辩论立场二维图（广度型↔深度型 × 隔离收益↔传递损耗）。agent loop 流程已有高质量 ASCII 图可保留。G5 表格已达标，整改补 1-2 张即可。
- **G10（正文代码"可拼凑运行"/节选标注）— 1 处系统性违反（4 个位置同根：讲解用节选未标"节选/示意"）**：
  1. 01 章 L95-105 `exec_bash` 节选：`env`、`BASH_TIMEOUT` 未定义，错误串以 `...` 截断——挂真函数签名却非可运行体；
  2. 01 章 L130-141 `parse_tool_calls` 节选：`...` 空体 + 注释行替代实现，与脚本同名同签名（更易误导）；
  3. 01 章 L150-155 回填片段：`"name": ..., "arguments": ...` 省略号入 dict；
  4. 01 章 L189-192 裁剪策略：`head + [...] + tail` 伪表达式。
  - 修法统一：各块首行加"（节选，完整实现见脚本 L1xx）"或改标"示意"；无需贴全文。✅ 正面：引用路径/文件全部真实存在、实测输出段未发现凭记忆写入（逐条对读吻合）。
- **G13（外部来源数字双列制）— 0 实锤 + 2 裁量（本章外部数字纪律全课程上游）**：15×/4×/90.2%/3-5 subagent 带原文引用+链接 ✅；Opus 4.5 80.9% 带"截至 2026-09+官方公告+以实时榜单为准"三重免责 ✅。裁量：① Anthropic 数字宜补披露时点（2025-06）；② "τ-bench 上游曾修复评分 bug"三处转述（02 章 L256、脚本 03 L13/L290）无具体 PR/commit 出处，宜补链接或标"社区流传口径"。
- **G14（实跑日志复现口径）— 0 实锤 + 1 轻项**：01 章 §3 头部环境声明（4090 24GB/fp16/transformers 4.57.6/torch 2.6.0+cu124/贪心/18.4s）+ "换设备浮点分岔"双设备声明 = 全课程样板；轻项：未写线程数；02 章 §5.1 τ-mini 实测口径缺 transformers/torch 版本（补一行即可）。
- **正面登记（无需整改）**：G5 表格 ✅；G6 教程数字与脚本输出三方一致（缺口 #3/#5 两处小漂移除外）✅；G12 无"失败仍庆祝"（pass^1=0.00 照实登）✅；G17/G18 作业同步与 SKIP 机制 ✅；G19 意识好（MCP 治理免责、版本协商免责、榜单实时性免责三处常设声明）。

## 六、跨 Part 一致性

| 对 | 检查项 | 结论 |
|---|---|---|
| P19↔P17（姊妹篇 O4） | 分工声明 | P19→P17 五处+声明、训练/推理侧边界清晰 ✅；P17→P19 零声明（已由 plan_P17 缺口 #6 登记，P17 侧修复） |
| P19↔P8（02 章 chat template） | 工具协议=chat template 扩展 | `apply_chat_template(messages, tools=...)` 口径一致 ✅；P8 链接实存 |
| P19↔P18（应用线 A1/A2） | 姊妹篇定位 | README"知识从哪来/行动怎么执行"分工 + 检索=工具类别之一的表述 ✅ |
| P19↔P14 | vLLM 演进路线 | "解析层已兼容 tool_calls，换 vLLM 循环零改动" ✅（可选前置，措辞不过度承诺） |
| P19↔roadmap v3 | 节点 19 / A2 | **roadmap 有 A2 专节（L555-564）且缺口登记 L595 已闭环 ✅**——内容/目标/验证三段与实际交付（手写 loop→mini-MCP→τ-mini→框架认知；4+1 作业）逐项对得上 ✅ |
| P19↔根 README | Part 摘要行 | L47/68/179 三处口径一致 ✅ |

## 七、预审缺口汇总（内容级实锤 5 项 + 降级核实 4 项；格式级见 §五）

| # | 级别候选 | 描述 | 位置 | 修复方案 |
|---|---|---|---|---|
| 1 | P1 | 陷阱 3 解法列"每轮调用数上限"，脚本 01 的调用循环无任何每轮上限（`for c in calls:`）——承诺-实现不符 | 01 章 L368 ↔ 脚本 01 L295 | 教程改"（脚本 03 的 `calls[:3]` 是它的最小实现；脚本 01 教学版未加）"或脚本 01 补 `calls[:N]` |
| 2 | P1 | bash 白名单只查第一词，`shell=True` 下 `;`/`&&`/反引号可绕过（`cat x; rm -rf /`）——安全教学点未提示该绕过面 | 教程 01 §2.1/陷阱 3 ↔ 脚本 01 L103-115 | 教程补半句"第一词检查可被 shell 元字符绕过，生产必须 shell=False/容器"；学生实测验证（P1 候选的主验证项） |
| 3 | P2 | §2.1 称 TOOL_SPECS 与 tools/list"同一 JSON schema 格式"，未提 OpenAI `function.parameters` vs MCP `inputSchema` 外壳键名差异（脚本 02 用的就是 inputSchema） | 02 章 L92 ↔ 脚本 02 L40-51 | 表格补一列/半句："外壳键名不同、内核同为 JSON Schema" |
| 4 | P2 | 政策文档"~300 字"实为 614 字符/100 词（英文） | 02 章 L203 ↔ 脚本 03 L40 注释 | 改"~100 词 / ~600 字符"或删量化 |
| 5 | P2 | 脚本 03 GPU 时长口径漂移：教程两处 ~15-25s vs docstring ~12-25s | README L8、02 章 L9 ↔ 脚本 03 L20 | 统一口径（建议 12-25s） |
| 6 | 降级核实 | Pi 博客 URL/日期（mariozechner.at 2025-11-30）、"千行代码实现"的 smolagents 官方口径 | 02 章 L145-151 | 联网期核对；失效则换存档链接 |
| 7 | 降级核实 | AgentRL arXiv 编号 2510.04206、GiGPO 2505.10978、τ²-bench 2506.07982 | README L91、02 章 L268-271 | 联网期核对编号-标题对应 |
| 8 | 降级核实 | "τ-bench 上游评分 bug"具体出处（PR/commit）；SWE-bench Opus 4.5 80.9% 榜单现值 | 02 章 L247-260、脚本 03 | 联网期核对并补引用 |

## 八、学生执行清单（各单元通用动作）

- [ ] 通读 README → 01 → 02 全文，按 §二 侧重记录卡点（🔴🟡🟢+位置+期望+实际+时长）
- [ ] `cp` 三脚本到 `REVIEW/scratch/<S>_P19/` 运行（G16）：01 逐字比对 §3、02 逐字比对 §2、03 跑两次看方差；模型下载失败时登记并降级 Section 0/0.5 + 脚本 02
- [ ] 安全实测（B 单元主责）：`bash("cat /tmp/agent_demo.txt; echo pwned")` 验证缺口 #2 绕过；NUDGE_BUDGET=0 重跑 Demo 3；WHITELIST+echo 重跑 Demo 4
- [ ] τ-mini 实测：两次完整运行记录 pass^1（对照教程 0.00/0.11 双口径）；R=6 复跑；定位 verify() 三查代码行
- [ ] 作业三件套在 `REVIEW/students/<S>/work/assignment_19/` 落盘，pytest 输出入报告（先做后看；未实现题 5 应 SKIP）
- [ ] 费曼自检三问：三终止条件各防什么；为什么判分看 DB 终态不看话术；subagent 收益来自哪里
- [ ] 禁读违例自查声明（assignment_reference/、REVIEW 他人产出、STUDENT_FEEDBACK/SUGGESTION/docs 2026-08-31 等）
