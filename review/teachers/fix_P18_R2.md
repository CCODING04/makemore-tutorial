# P18-C01《从朴素 RAG 到混合检索：手写五件套》优化说明（试点轮 R2）

> **轮次**：三章试点 R2 ｜ **日期**：2026-09-05 ｜ **状态**：待用户评审
> **改动文件**（影子工作区镜像，仓库未动）：
> - `courses/Part18_rag/tutorial/01_naive_to_hybrid.md`（589 → 约 640 行，格式规范 G1-G5 全量应用）
> - `courses/Part18_rag/images/recall_ablation.png`（新增，matplotlib 生成）
> - `assignments/assignment_18/assignment.md`（G1 拆行 + 题 5 公式 LaTeX 化）
> **不改**：`scripts/01_minimal_rag.py`——脚本本身无缺陷（教程代码块错在"简化失真"，脚本是对的）。

---

## 一、与 R1 的关键差异：这章的实测基础非常扎实

全量模式 **recall 表 16 格、cosine 0.6566、238 chunks（93/420/512）、总耗时 13.5s、生成回答摘录**
由教师 + S2 + S3 **三方独立复现逐格一致**；降级块 4 格一致；size=180 消融的
4 个数字 S3 自写评测 driver 复算全部对上。**C3（代码已执行且结果正确）满分通过**——
这与 R1（数字全对但口径失真）形成对照，说明"快照固定"策略是有效的。

## 二、修改明细（F01-F14）

### P0（必修，3 项）

| # | 问题 | 来源 | 修复 | 验证 |
|---|---|---|---|---|
| F01 | **教程 `recursive_chunk` 代码块与脚本不一致**：缺 `'。'` 捕获组行与空文本早退。`s.split('。')` 丢句号、破坏"不丢字符"不变量③——**S2 照抄教程版做作业题 1 实测爆炸**（本轮最硬的一条实证） | S2🔴+教师预读 | 教程代码块替换为脚本真身（逐行一致），并加 ⚠️ 教训框写明"省掉那一行会怎样" | S2 作业复现 + 一致性核对表归零 |
| F03 | **BM25 推导自相矛盾**："`IDF` 在 df→N 时趋于 0 甚至为负"，但自己给的 `log(N/df)` 恒非负——数学弱学生对着公式自我怀疑 5 分钟（S1 全章最长卡点） | S1🔴+教师数学核验 | 改为点破"会变负的是未加 1 的变体 `log((N-df+0.5)/(df+0.5))`，Step 4 的 +1 平滑正是修它" | df=N → log(1)=0 手验 |
| F02 | **三处数学全装在 ASCII 代码块里**（无渲染、无数字例、符号不定义），违反已转正的格式规范 G2；S1 第一遍直接跳过整节 | S1🔴+规范 | 全部移出代码块改 LaTeX（`$$` 单行、公式内无中文、列表内行内公式）；新增符号表（N=238 chunks 等）、TF 饱和数字感受表（tf=1/3/10/100→1.00/1.57/1.96/2.17）、BM25 贯穿小算例（IDF≈3.13 × TF 因子 1.26 ≈ 3.9 分） | `check_latex.py` 0 问题（还抓到并修掉了我自己写的 RRF 公式里 4 处中文） |

### P1（10 项）

| # | 问题 | 来源 | 修复 |
|---|---|---|---|
| F04 | RRF 极限"≈"一步缺泰勒推导与适用条件 | S1🟡 | 补 `(1+r/k)^-1 ≈ 1-r/k (r≪k)` + 误差 <1% 验证 + "先比入选数再比名次和"的排序结论 |
| F05 | Q1 答案"TF 权重被压一半"不精确（实算 0.65 倍） | S1🟡 | 改"约 0.65 倍（tf 大时才趋近一半）"+算式 2.2/(1+2.4) |
| F06 | "因子为 1"歧义（TF 因子还是长度括号？） | S1🟡 | 写明指分母括号长度部分，附 1.75 倍数值例 |
| F07 | "hashing trick 的价值"①② 与概念检验 Q2 答案①② 挤行（G1） | 教师 | 拆为列表项 |
| F09 | IDF 平滑 +0.5 无解释、ln/log 底数困惑 | S1🟡 | 注明 Okapi 标准平滑 + 底数不影响排序 |
| F10 | "错误 1"复现指引缺关键分支：有网环境会静默下载 1.2GB 模型（S2 实测 130s 未完成）而非触发降级 | S2🟡 | 补 `HF_HUB_OFFLINE=1` 复现命令 |
| F11 | "Weaviate/Elastic 同年内置 RRF（2023）"无出处且对 Weaviate 是错的 | S3🟡+教师核实 | 改为核实后时间线：Weaviate v1.15（2021-10）即带 rank fusion、ES 8.8（2023-06）technical preview，附官方链接两处 |
| F12 | 重排 ~0.3s/生成 ~1s 无日志可复核（S3：面试报小数点会被追问） | S3🟡 | 性能表加 * 注"量级参考，脚本无逐阶段计时"，环境标注块同步声明 |
| F13 | 陷阱 1"plain recall@20"口径未定义；size 消融非严格单变量（GT 命中集随 chunk 数变） | S3🟡 | 定义 plain=dense 单路@20+声明 4/14/9→5/19/18 的 GT 漂移；练习 3 加"对照连 GT 一起看"验收项 |
| F14 | 作业 Q4 答案"三关"①②③挤一行（G1）+ 题 5 加权公式 LaTeX 化 | 教师 | 拆为列表、改 `$w·dense+(1-w)·sparse$` |

### P2（2 项）

| # | 改动 |
|---|---|
| F08 | 新增 recall 四级消融分组柱状图（G4；数字即被三方复现的表格，图注中文、图内英文）——"逐级抬升"可视化 |
| — | 降级嵌入耗时"0.4s"→"~0.5s"（S2 实测波动，不锁死数字） |

## 三、验证记录

1. **LaTeX 机检**：`check_latex.py` 对教程+作业均 0 问题（首跑抓出 4 处公式内中文，已修）。
2. **数字三方复现**：见 §一；优化版未改任何教程数字，只改表述、口径标注与图示来源说明。
3. **图**：`make_recall_fig.py` 可复现；图中 16 个数字与表格逐格一致。
4. **教程↔脚本一致性**：F01 修复后 5 个函数代码块逐行一致（S2 核对表）。

## 四、未采纳/保留

- **不给脚本加逐阶段计时日志**（可让 F12 可复核）：会改脚本输出格式，破坏"教程输出块逐字符可复现"；用表注声明代替。全量阶段若统一接受"加计时列"，再作为脚本改进批量处理。
- **02 脚本 docstring 的 Anthropic 数字（1.9% vs 官方 2.1%）**：属 C02 范围，台账记 `deferred(C02)`，留给下一章/全量阶段对照原文统一处理。
- **README 导航行缺参考时长**：Part 级问题，不属本章，全量阶段统一补。

## 五、请用户重点评审的 3 个决策点

1. **F02 的"数学节重写"尺度**：推导从代码块改为 LaTeX + 符号表 + 3 处数字感受表 + 1 个贯穿算例（篇幅 +60 行）。这是对 S1 画像的正面响应，但也让理论节变长——是否认可这种"为数学弱读者加密度"的方向？
2. **F11 历史声明改写**：原文"2023 同年内置"改为"Weaviate 2021 / ES 8.8 2023 preview"并附官方链接——教程里所有历史声明今后是否一律要求"年份+版本+可点出处"？
3. **F12 的处理哲学**：教程引用数字但脚本日志无法复核时，选择"表注声明量级参考"而非"改脚本加日志"——"数字可溯源"与"输出逐字符稳定"冲突时优先后者，同意吗？
