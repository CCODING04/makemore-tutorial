# 核心依赖
torch>=1.10      # PyTorch，版本 1.10+ 或 2.x 均可
matplotlib>=3.0  # 绘图库
numpy>=1.20      # 数值计算（torch 会自动安装，但显式声明更清晰）

# 可选依赖
pytest           # 用于运行作业测试（python test_xxx.py）
tokenizers       # Part 7 BPE 分词器训练（未安装时脚本自动回退字符级）
ninja            # Part 9 PyTorch CUDA 扩展 JIT 编译需要（有 GPU + nvcc 时）
transformers<5   # Part 14 推理基线（钉 4.x：4.57.6 在本课环境实测通过；5.x 为破坏性大版本，未验证，勿自动升级）
lm_eval[hf]      # Part 8 评估实操（v0.4.10+ 默认不装 HF 栈，必须 extras；实测 0.4.13）
ragas           # Part 18 RAG 评测对照（可选：未安装时脚本 03 打印指引后跳过该段）
scipy           # Part 13 scaling law 拟合（fit_chinchilla 用 least_squares + Huber）

# 📦 Part 8/18 需要的 HF 模型（首次运行自动下载，也可预取；详见 docs/datasets.md）：
#    Qwen/Qwen2.5-0.5B-Instruct（生成/裁判/judge）  Qwen/Qwen2.5-0.5B（base，校准对比）
#    Qwen/Qwen3-Embedding-0.6B（Part 18 嵌入）      BAAI/bge-reranker-v2-m3（Part 18 重排）
#    缺模型时脚本均有降级路径（hashing trick / 跳过重排 / 提示退出），不会崩溃

# ⚠️ uv 创建的 venv 默认不含 setuptools，CUDA 扩展 JIT 编译（Part 9）前需显式安装：
#    uv pip install setuptools ninja
